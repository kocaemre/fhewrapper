---
phase: 05-unwrap-async-finalize
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - packages/nextjs/components/unwrap/UnwrapStageIndicator.tsx
  - packages/nextjs/components/unwrap/UnwrapPanel.tsx
  - packages/nextjs/app/unwrap/page.tsx
  - packages/nextjs/components/wrap/WrapPanel.tsx
  - packages/nextjs/components/registry/PairCard.tsx
autonomous: true
requirements: [UNW-01, UNW-02]
tags: [unwrap, ui, next-app-router, chainguard, honest-async, resume, loop-closure]

must_haves:
  truths:
    - "A judge can open /unwrap?token=<confidential> for a registry pair, enter an amount ≤ their decrypted confidential balance OR press Unwrap all, and submit the unwrap under ChainGuard (UNW-01)."
    - "The UI shows an explicit Request → Decrypting → Finalize → Done progression; the Done/success state appears ONLY after the finalize resolves — never at the unwrap-submit step (UNW-02)."
    - "On finalized, the Phase-3 decrypt shows the confidential balance dropped AND wagmi shows the underlying ERC-20 balance increased — the honest end-state proof."
    - "The Wrap/Unwrap toggle (WrapPanel) and the pair surface reach /unwrap, making the wrap → decrypt → unwrap loop continuous (SC4)."
    - "An interrupted burned-but-unfinalized unwrap surfaces a Resume affordance that finalizes via useResumeUnshield (never-strand-funds)."
  artifacts:
    - packages/nextjs/components/unwrap/UnwrapStageIndicator.tsx
    - packages/nextjs/components/unwrap/UnwrapPanel.tsx
    - packages/nextjs/app/unwrap/page.tsx
  key_links:
    - "/unwrap?token= → trusted useRegistryPairs pair → UnwrapPanel under ChainGuard + Suspense"
    - "UnwrapPanel amount Max ← Phase-3 useUserDecrypt decrypted confidential balance"
    - "stage === finalized → refetch ERC-20 balanceOf (wagmi) + render PairCardDecrypt (honest proof)"
    - "WrapPanel Unwrap tab + PairCard → /unwrap?token=<confidential> (loop continuity, SC4)"
---

<objective>
Make the honest unwrap reachable and close the headline loop: a `/unwrap` screen (mirroring `/wrap`) where a judge picks an amount from their Phase-3 decrypted confidential balance (or Unwrap-all with no decrypt), submits, and watches an honest Request → Decrypting → Finalize → Done indicator that only reports success once the finalize resolves and the underlying ERC-20 arrives (UNW-01/UNW-02). Then wire the Wrap/Unwrap toggle and pair surface into it so wrap → decrypt → unwrap is a continuous loop (SC4), and surface a Resume affordance for an interrupted finalize.

Purpose: This is the user-facing slice — after it, a judge can actually run unwrap end-to-end and see the ERC-20 return, completing the loop Phase 4 opened.
Output: `components/unwrap/UnwrapStageIndicator.tsx`, `components/unwrap/UnwrapPanel.tsx`, `app/unwrap/page.tsx`, and the Wrap/Unwrap toggle + PairCard wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-unwrap-async-finalize/05-RESEARCH.md
@.planning/phases/05-unwrap-async-finalize/05-01-PLAN.md

# Mirror-the-shape sources (this plan parallels the Phase-4 wrap surface exactly):
@packages/nextjs/components/wrap/WrapPanel.tsx
@packages/nextjs/components/wrap/WrapStageIndicator.tsx
@packages/nextjs/app/wrap/page.tsx
@packages/nextjs/components/registry/PairCard.tsx

# Reused engines / components:
@packages/nextjs/hooks/useUnwrap.ts
@packages/nextjs/hooks/useUserDecrypt.ts
@packages/nextjs/components/decrypt/PairCardDecrypt.tsx
@packages/nextjs/lib/unwrapAmount.ts
@packages/nextjs/lib/unwrapErrors.ts
@packages/nextjs/lib/pendingUnshield.ts

# Verified: useResumeUnshield(config,options) @ node_modules/@zama-fhe/react-sdk/dist/index.d.ts:740
#   config = { tokenAddress, wrapperAddress }; param ResumeUnshieldParams { unwrapTxHash: Hex } + UnshieldCallbacks
</context>

<tasks>

<task type="auto">
  <name>Task 1: UnwrapStageIndicator — honest 4-stage Request → Decrypting → Finalize → Done</name>
  <files>packages/nextjs/components/unwrap/UnwrapStageIndicator.tsx</files>
  <action>Copy the visual structure of `components/wrap/WrapStageIndicator.tsx` but drive it off `UnwrapStage` from `~~/lib/unwrapStages` and relabel the four steps to the honest unwrap flow so the async wait is explicit (Pitfall 2 — never a bare spinner): `["requesting", "Request", "burn + request decrypt"]`, `["decrypting", "Decrypting", "oracle publicly decrypts"]`, `["finalizing", "Finalize", "release ERC-20"]`, `["finalized", "Done", "ERC-20 arrived"]`. Derive the current index from the stage (`idle`/`error` show all steps pending); a completed step shows the ✓ glyph green, the active step pulses red (reuse the existing `inkPulse` animation and the same `--green`/`--red`/`--line` CSS variables), pending steps muted — identical treatment to the wrap indicator. Keep it a pure presentational client component `{ stage }: { stage: UnwrapStage }`. The critical honesty point: the "Done" step is only reached when `stage === "finalized"` (the reducer only reaches it on the finalize resolve), so this indicator can never show completion at the request/decrypt/finalize steps.</action>
  <verify>
    <automated>cd packages/nextjs && npm run check-types</automated>
  </verify>
  <done>Component type-checks; renders four honest stages with the explicit Decrypting (oracle-wait) step; the Done step is reachable only at `finalized`. No new CSS/animation introduced (reuses inkPulse + existing vars).</done>
</task>

<task type="auto">
  <name>Task 2: UnwrapPanel + /unwrap route — amount from decrypted balance, Unwrap-all, honest end-state proof, resume</name>
  <files>packages/nextjs/components/unwrap/UnwrapPanel.tsx, packages/nextjs/app/unwrap/page.tsx</files>
  <action>Build `UnwrapPanel({ pair }: { pair: RegistryPair })` mirroring `WrapPanel` in layout/theme but reversed (From · Confidential ERC-7984 → To · Public ERC-20). Amount source (UNW-01): use the Phase-3 `useUserDecrypt(confidential.address, ...)` to obtain the decrypted confidential balance `value: bigint`; render a Reveal/Decrypt affordance (reuse its stage) and, once revealed, offer "Max = decrypted balance". Validate the typed amount with `parseUnwrapAmount(amount, confidential.decimals, decryptedValue)` (from 05-01) — disable the primary button when `!valid`, and show the `exceedsBalance`/`belowMinimum` reasons inline. Provide TWO actions from `useUnwrap(confidential.address)` (05-01): the primary "Unwrap <cSymbol>" runs `unwrap(parsedAmount.raw)`; a secondary "Unwrap all" runs `unwrapAll()` (no decrypt needed — research Pattern 3) so a judge who hasn't decrypted can still unwrap the whole balance. Render `<UnwrapStageIndicator stage={stage} />` and, on `error`, an error row via `toUnwrapError(e)` (no raw revert; identical markup to WrapPanel's error row). Honest DONE (UNW-02): read the underlying ERC-20 balance with wagmi `useReadContract({ address: underlying.address, abi: erc20Abi, functionName: "balanceOf", args:[account], chainId: sepolia.id })`; when `stage === "finalized"`, call its `refetch()` and render BOTH the new ERC-20 balance (increased) AND the Phase-3 `<PairCardDecrypt pair={pair} />` (confidential dropped) as the visible end-state proof — success copy appears ONLY in this `finalized` block, never earlier. Resume affordance (Pitfall 3): on mount, `readPendingUnwrap(confidential.address)` (05-01); if a pending unwrap tx hash exists and the panel is idle, show a "Resume interrupted unwrap" banner whose button calls `useResumeUnshield({ tokenAddress: confidential.address, wrapperAddress: confidential.address }).mutateAsync({ unwrapTxHash })` wired to the SAME honest stage callbacks, then `forgetPendingUnwrap` on resolve. Then `app/unwrap/page.tsx`: copy `app/wrap/page.tsx` — resolve the pair from the trusted `useRegistryPairs` by `?token=<confidential address>` (never trust the query param as a contract beyond matching a known registry pair), render `<UnwrapPanel pair={pair} />` inside `<Suspense>` (useSearchParams boundary) under the Phase-1 `ChainGuard` (write flow → require connection + Sepolia, T-05-03). Do NOT place fenced code in components; keep the Cellar-engraving inline-style theme identical to WrapPanel (functional-first; visual polish is Phase 7).</action>
  <verify>
    <automated>cd packages/nextjs && npm run build && npx vitest run</automated>
  </verify>
  <done>`next build` emits the `/unwrap` route (static, Suspense-wrapped, under ChainGuard); full vitest suite green. Panel: amount capped at the decrypted balance, Unwrap-all present, honest indicator, typed error row, on-`finalized` ERC-20-increased + decrypt proof shown only at finalized, and a Resume banner when a pending unwrap exists.</done>
</task>

<task type="auto">
  <name>Task 3: Close the loop — enable the Wrap/Unwrap toggle + pair surface into /unwrap (SC4)</name>
  <files>packages/nextjs/components/wrap/WrapPanel.tsx, packages/nextjs/components/registry/PairCard.tsx</files>
  <action>In `components/wrap/WrapPanel.tsx`, replace the currently-disabled "Unwrap" affordance (the `title="Unwrap ships in Phase 5"` span, cursor not-allowed) with a functional `next/link` to `/unwrap?token=${confidential.address}` styled identically to the active "Wrap" tab treatment — so the Wrap/Unwrap toggle actually switches surfaces (loop continuity, SC4). In `components/registry/PairCard.tsx`, add an "Unwrap →" affordance for valid pairs alongside the existing "Wrap →" CTA (a sibling `next/link` to `/unwrap?token=${confidential.address}`, same button styling), so a judge can enter unwrap directly from the ledger for a token they just wrapped. Keep revoked pairs' "Unavailable" state unchanged. Do not alter the decrypt Section 4 or the address rows. Onchain `name`/`symbol` stay JSX-escaped (T-02-01 carried).</action>
  <verify>
    <automated>cd packages/nextjs && npm run build && npm run check-types</automated>
  </verify>
  <done>The WrapPanel Unwrap tab links to `/unwrap?token=`; valid PairCards expose an "Unwrap →" link to `/unwrap?token=`; `next build` + `check-types` green; the wrap → decrypt → unwrap loop is navigable end-to-end in-app (live proof → 05-UAT).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `?token=` query param → /unwrap | Untrusted URL input; must be matched against a trusted registry pair, never used as a raw contract address. |
| user → write flow (unwrap/finalize/resume) | Wallet-signed txs on Sepolia; gated on connection + correct network. |
| finalize proof → wrapper | The KMS/relayer cleartext+proof are verified onchain by the wrapper before it releases ERC-20. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Repudiation/Info | UnwrapPanel finalized block | high | mitigate | Success copy + the "ERC-20 arrived" proof render ONLY inside the `stage === "finalized"` block; on finalize the ERC-20 `balanceOf` is refetched and the Phase-3 decrypt shows the confidential drop — no optimistic success anywhere earlier (UNW-02). |
| T-05-05 | Tampering | /unwrap ?token= resolution | medium | mitigate | The pair is resolved from the trusted onchain `useRegistryPairs` by matching the confidential address; the query param is never used directly as a contract target (mirrors /wrap T-04-09). |
| T-05-03 | Elevation/DoS | write gating | medium | mitigate | `/unwrap` renders under the Phase-1 `ChainGuard` — connection + Sepolia required before any mutate; wrong-network disables actions. |
| T-05-06 | DoS/UX | interrupted finalize | medium | mitigate | A persisted pending unwrap surfaces a Resume banner (`useResumeUnshield`); the confidential burn is recoverable rather than silently stranded (Pitfall 3). |
| T-05-02 | Tampering | amount input | low | mitigate | Reuses `parseUnwrapAmount` (05-01) — decimals-driven, capped at the decrypted balance; button disabled when invalid. |
| T-05-SC | Tampering | npm installs | low | accept | No new packages this plan (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `cd packages/nextjs && npm run build` — `/unwrap` route emitted; `npm run check-types` exit 0; `npx vitest run` full suite green.
- In-app the Wrap/Unwrap toggle and PairCard "Unwrap →" navigate to `/unwrap?token=`, and the panel shows the honest Request → Decrypting → Finalize → Done progression with the finalized-only end-state proof.
- The live full wrap → decrypt → unwrap → ERC-20-arrives loop (SC1-4) is wallet/relayer-owned → 05-UAT (deferred to the end-of-project session).
</verification>

<success_criteria>
- `/unwrap` is a reachable, ChainGuard-gated screen with amount-from-decrypted-balance, Unwrap-all, honest 4-stage indicator, typed errors, finalized-only end-state proof, and a Resume affordance.
- The Wrap/Unwrap toggle + PairCard link the loop continuously (SC4).
- Zero new dependencies; build + check-types + full vitest green.
</success_criteria>

<output>
Create `.planning/phases/05-unwrap-async-finalize/05-02-SUMMARY.md` when done.
</output>
