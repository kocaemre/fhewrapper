---
phase: 07-polish-animation-submission
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/nextjs/hooks/index.ts
  - packages/nextjs/components/decrypt/DecryptStateBox.tsx
  - packages/nextjs/components/decrypt/PairCardDecrypt.tsx
  - packages/nextjs/styles/globals.css
autonomous: true
requirements: [DIF-03, DIF-04, DIF-05]

must_haves:
  truths:
    - "A single hooks/index.ts barrel exports the clean public API — useRegistry (aliasing useRegistryPairs), useWrap, useUnwrap, useUserDecrypt (+ useFaucet) with their result/stage types — so the code-quality axis reads as a deliberate public surface (DIF-05)"
    - "Decrypting any ERC-7984 balance shows the blurred ciphertext transitioning to the cleartext in place (blur to reveal), on both the /decrypt panel and the registry per-card decrypt, with a prefers-reduced-motion fallback (DIF-03)"
    - "The registry browser exposes search, valid/revoked filter, valid/revoked badges, and copy-address buttons — all present and wired into the pair cards (DIF-04)"
    - "check-types is clean and the full vitest suite stays green after the barrel + polish"
  artifacts:
    - "packages/nextjs/hooks/index.ts — the typed public hooks barrel (DIF-05)"
  key_links:
    - "hooks/index.ts re-exports the four canonical hooks + types without changing their signatures (public API, not a rewrite)"
    - "DecryptStateBox/PairCardDecrypt cipher-blur to reveal-num transition consumes the existing motion CSS with a reduced-motion fallback"
---

<objective>
Finalize the code-quality and micro-interaction polish axes: (1) DIF-05 — expose a clean, well-typed public hooks API via a `hooks/index.ts` barrel that re-exports the four canonical reusable hooks (`useRegistry` aliasing `useRegistryPairs`, `useWrap`, `useUnwrap`, `useUserDecrypt`, plus `useFaucet`) together with their exported result/stage types, without changing any signature. (2) DIF-03 — verify and tighten the decrypt reveal micro-interaction (blurred ciphertext decrypting in place) on both the `/decrypt` panel and the registry per-card decrypt, keeping a `prefers-reduced-motion` fallback. (3) DIF-04 — verify the registry browser polish (search, valid/revoked filter + badges, copy buttons) is present and wired, and apply a final visual tighten where warranted.

Purpose: DIF-05 is an explicit code-quality judging axis; DIF-03/DIF-04 were largely delivered in Phases 2-3, so this plan proves they hold and tightens the finish rather than rebuilding them.
Output: `hooks/index.ts` (new public barrel) plus any tightening to the decrypt reveal (`DecryptStateBox`/`PairCardDecrypt`/`globals.css`).
</objective>

## Phase Goal

**As a** bounty judge reviewing code and using the app, **I want to** import clean typed hooks and see confidential balances decrypt in place with a polished blur-to-reveal, **so that** the submission reads as production-grade on both the code-quality and interaction axes.

> Derived from the ROADMAP Phase 7 goal + SC2/SC3 (discuss skipped under the floor-it directive; user asleep).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-animation-submission/07-CONTEXT.md

# The four canonical hooks the barrel re-exports (signatures + exported types):
@packages/nextjs/hooks/useRegistryPairs.ts
@packages/nextjs/hooks/useWrap.ts
@packages/nextjs/hooks/useUnwrap.ts
@packages/nextjs/hooks/useUserDecrypt.ts
# The existing blur-to-reveal micro-interaction (DIF-03, built Phase 3):
@packages/nextjs/components/decrypt/DecryptStateBox.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Public hooks API barrel (DIF-05)</name>
  <files>packages/nextjs/hooks/index.ts</files>
  <action>Create `hooks/index.ts` as the deliberate public API surface for the reusable hooks (DIF-05, code-quality axis). Re-export the four canonical hooks and their exported types: `useRegistryPairs` (also re-exported as `useRegistry` to match the requirement's naming) with `UseRegistryPairsResult`; `useWrap` with `UseWrapResult`, `WrapStage`, `ApprovalStrategy`; `useUnwrap` with `UseUnwrapResult`; `useUserDecrypt` with `UseUserDecryptResult`, `UseUserDecryptOptions`, `DecryptStage`; and `useFaucet`. Add a top-of-file JSDoc block documenting this as the project's public hook API and one-line summaries of each hook's contract (what it returns, what onchain state it reads/writes). Do NOT change any hook signature or move any file — this is a re-export barrel only. Prefer `export { ... } from` and `export type { ... } from` so the alias and the type-only re-exports stay clean under isolatedModules. Verify with a strict type check that the barrel resolves every symbol.</action>
  <verify>
    <automated>cd packages/nextjs && grep -q 'useRegistry' hooks/index.ts && grep -q 'useWrap' hooks/index.ts && grep -q 'useUnwrap' hooks/index.ts && grep -q 'useUserDecrypt' hooks/index.ts && npm run check-types</automated>
  </verify>
  <done>hooks/index.ts re-exports all four canonical hooks (with useRegistry alias) + their types; check-types is clean; no hook signature changed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify + tighten the reveal (DIF-03) and registry polish (DIF-04)</name>
  <files>packages/nextjs/components/decrypt/DecryptStateBox.tsx, packages/nextjs/components/decrypt/PairCardDecrypt.tsx, packages/nextjs/styles/globals.css</files>
  <action>DIF-03 (reveal micro-interaction): confirm the blur-to-reveal exists on both the /decrypt panel (`DecryptStateBox` — the `cipher-blur` blurred ciphertext transitioning to the `reveal-num` cleartext) and the registry per-card decrypt (`PairCardDecrypt`). Tighten the transition so the cleartext eases into place when the stage moves to revealed (a short opacity/blur-out on the ciphertext into a settle on the number), and ensure the `prefers-reduced-motion` fallback in `globals.css` still renders an instant, non-animated reveal (no motion for reduced users). Keep the existing state-chip copy and the DEC-04 zero-balance-as-revealed behavior unchanged. DIF-04 (registry polish): verify the registry browser already exposes search + valid/revoked filter (`RegistryToolbar`), valid/revoked badges (`PairBadge`), and copy-address buttons (`AddressCopyButton`) wired into `PairCard`; if any affordance is missing or visually rough, tighten it, otherwise leave the working components as-is (do not rebuild what Phase 2 delivered). Run the full test suite + build to prove no regression. Per DIF-03/DIF-04 and 07-CONTEXT decision D.</action>
  <verify>
    <automated>cd packages/nextjs && grep -q 'cipher-blur' components/decrypt/DecryptStateBox.tsx && grep -q 'reveal-num' components/decrypt/DecryptStateBox.tsx && grep -q 'PairBadge' components/registry/PairCard.tsx && grep -q 'AddressCopyButton' components/registry/PairCard.tsx && grep -qi 'search' components/registry/RegistryToolbar.tsx && npx vitest run && npm run build</automated>
  </verify>
  <done>Blur-to-reveal present + tightened on both decrypt surfaces with a reduced-motion fallback; registry search/filter/badge/copy affordances confirmed wired into PairCard; full vitest suite green; next build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public hooks API to external consumers | The barrel is the documented reusable surface a judge or downstream dev imports; a leaky/incorrect re-export misrepresents the API |
| decrypted cleartext to the DOM | The reveal renders a user's decrypted confidential balance client-side (already local-only via the EIP-712 permit) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Tampering | hooks/index.ts barrel | low | mitigate | Re-export only (no signature changes); check-types must resolve every symbol so the public API cannot drift from the real hooks |
| T-07-08 | Information | decrypt reveal in DOM | low | accept | Cleartext is decrypted locally under the existing per-token EIP-712 permit (Phase 3) and never broadcast; the reveal is display-only and this plan changes only the transition, not the data path |
</threat_model>

<verification>
- `npm run check-types` — the barrel resolves every re-exported hook + type (DIF-05).
- Grep gates: barrel exports useRegistry/useWrap/useUnwrap/useUserDecrypt; DecryptStateBox keeps cipher-blur + reveal-num; PairCard wires PairBadge + AddressCopyButton; RegistryToolbar has search.
- Full `npx vitest run` + `npm run build` — no regression from the polish.
- Deferred to 07-UAT (live URL): the blur-to-reveal reads smoothly on a real decrypt; search/filter/badges/copy behave on the deployed registry.
</verification>

<success_criteria>
- `hooks/index.ts` is the clean, typed public API (useRegistry/useWrap/useUnwrap/useUserDecrypt + types); check-types clean.
- Decrypt blur-to-reveal present + tightened on both surfaces with a reduced-motion fallback (DIF-03).
- Registry search/filter/badges/copy confirmed wired (DIF-04).
- Full vitest suite + `next build` green.
</success_criteria>

<output>
Create `.planning/phases/07-polish-animation-submission/07-03-SUMMARY.md` when done.
</output>
