---
phase: 07-polish-animation-submission
plan: 04
type: execute
wave: 2
depends_on: [07-01, 07-02, 07-03]
files_modified:
  - README.md
  - .planning/phases/07-polish-animation-submission/07-UAT.md
autonomous: true
requirements: [SUB-01, SUB-02, SUB-04, SUB-05]

must_haves:
  truths:
    - "The README documents the live URL (https://fhewrapper-nextjs.vercel.app/), the supported network (Sepolia / 11155111), how the registry is sourced (onchain Wrappers Registry + hybrid local config), how to add a pair, and the deploy guide (GitHub to Vercel, Root Directory packages/nextjs, npm, NEXT_PUBLIC_IPFS_BUILD unset) (SUB-02)"
    - "The README documents the exact SDK 3.0.0 pin and the COOP/COEP require-corp cross-origin-isolation requirement, and points to the clean reusable hooks public API"
    - "07-UAT.md captures the DEFERRED human/outward submission tasks — flip repo public (SUB-01), record the 3-minute real-person pitch video (SUB-04), publish the X thread/article (SUB-05) — plus the single end-of-project live-URL manual verification pass"
  artifacts:
    - "README.md — the project (The Cellar Registry) submission README (SUB-02)"
    - ".planning/phases/07-polish-animation-submission/07-UAT.md — the user's deferred outward + live-verification checklist"
  key_links:
    - "README deploy section reflects the real Phase-1 GitHub to Vercel pipeline (Root Directory packages/nextjs, npm, IPFS build unset)"
    - "07-UAT.md is the single hand-off surface for every non-autonomous item (repo public, pitch video, X thread, live checks)"
---

<objective>
Complete the submission documentation package. Rewrite the root `README.md` so it describes THIS project — The Cellar Registry, a confidential ERC-20 to ERC-7984 wrapper registry dApp — with the live URL, supported network, how the onchain registry is sourced, how to add a pair, the concrete deploy guide, the exact SDK 3.0.0 pin + COOP/COEP requirement, and the clean reusable hooks public API (SUB-02). Then create `07-UAT.md` to capture every DEFERRED human/outward task the floor-it directive assigns to the user: flipping the repo public (SUB-01), recording the 3-minute real-person pitch video (SUB-04), publishing the X thread/article (SUB-05), and running the single end-of-project live-URL verification session (cinematic on a real wrap, audio unlock, media under COEP, full wrap to decrypt to unwrap loop).

Purpose: SUB-02 is a required deliverable and the README is the judge's entry point; the deferred SUB-01/04/05 items are hard-to-reverse or human-only (a real-person video, a public reveal, a social post) and must be tracked, scripted, and handed to the user rather than done autonomously while they sleep.
Output: the rewritten `README.md` and the new `07-UAT.md` deferred-tasks checklist.
</objective>

## Phase Goal

**As a** bounty judge arriving at the repo, **I want to** read a README that gets me to the live app, explains how it works, and shows me how to deploy and extend it, **so that** I can evaluate and trust the submission in minutes.

> Derived from the ROADMAP Phase 7 goal + SC4 (discuss skipped under the floor-it directive; user asleep).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-polish-animation-submission/07-CONTEXT.md

# Current README (template-derived; the registry/add-a-pair section is reusable — extend it):
@README.md
# The deploy constraints (COOP/COEP + IPFS build flag) live here:
@packages/nextjs/next.config.ts
# The prior phase UAT format to mirror:
@.planning/phases/06-error-handling-status/06-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the submission README (SUB-02)</name>
  <files>README.md</files>
  <action>Rewrite the root `README.md` so its subject is The Cellar Registry (this bounty submission), not the generic FHEVM template. Keep and extend the already-accurate "Wrapper registry / How the registry is sourced / Adding a wrapper pair" section (it satisfies REG-07 and the add-a-pair requirement — do not regress it). Add/lead with: a one-paragraph project summary (confidential ERC-20 to ERC-7984 wrapper registry on Sepolia; browse the onchain registry, wrap/unwrap, decrypt any ERC-7984 balance via EIP-712, claim faucet tokens); the LIVE URL https://fhewrapper-nextjs.vercel.app/ prominently near the top; supported network Sepolia testnet (chain id 11155111). Add a "Deploy" section documenting the real Phase-1 pipeline: GitHub import to Vercel, Vercel Root Directory set to packages/nextjs, npm (two lockfiles exist — force npm, not pnpm/yarn), and NEXT_PUBLIC_IPFS_BUILD left unset (static export drops the COOP/COEP headers). Add a "Cross-origin isolation" note explaining the required Cross-Origin-Opener-Policy: same-origin + Cross-Origin-Embedder-Policy: require-corp headers (so crossOriginIsolated === true for the FHE WASM worker) and that ALL media is self-hosted for that reason. Add a "Stack / versions" note calling out the EXACT-pinned @zama-fhe/sdk + @zama-fhe/react-sdk 3.0.0 (no caret) and why. Add a short "Reusable hooks" section pointing at the public hooks API barrel (hooks/index.ts: useRegistry, useWrap, useUnwrap, useUserDecrypt). Keep the License section. Per SUB-02 and 07-CONTEXT decision E.</action>
  <verify>
    <automated>cd /Users/0xemrek/Desktop/fhewrapper && grep -q 'fhewrapper-nextjs.vercel.app' README.md && grep -q '11155111' README.md && grep -qi 'require-corp' README.md && grep -q 'packages/nextjs' README.md && grep -qi 'add' README.md</automated>
  </verify>
  <done>README covers live URL, Sepolia 11155111, registry sourcing, add-a-pair, the GitHub-to-Vercel deploy guide (Root Directory packages/nextjs, npm, IPFS build unset), the COOP/COEP require-corp note, the exact SDK 3.0.0 pin, and the reusable hooks API.</done>
</task>

<task type="auto">
  <name>Task 2: Create the deferred submission + live-verification UAT (SUB-01, SUB-04, SUB-05)</name>
  <files>.planning/phases/07-polish-animation-submission/07-UAT.md</files>
  <action>Create `07-UAT.md` (mirror the 06-UAT.md front-matter + section style) as the single hand-off checklist for every item the floor-it directive defers to the USER — these are outward/human/live and must NOT be done autonomously. Include front-matter (phase, type: manual-uat, status: pending, deferred: true, when: end-of-project before final submission, url, requirements: [SUB-01, SUB-04, SUB-05]). Sections: (1) Preconditions — latest commit deployed; crossOriginIsolated === true; MetaMask on Sepolia; a wallet with a wrapped confidential balance; and a security pre-check that NO secret/.env is committed before going public. (2) SUB-01 Flip the repo to PUBLIC — the explicit, hard-to-reverse step the user takes when ready (github.com/kocaemre/fhewrapper), with the secret-scan pre-check gating it. (3) SUB-04 Record the 3-minute REAL-PERSON pitch video — a shot list/script covering browse to faucet to wrap to decrypt to unwrap to decrypt-an-out-of-registry-ERC-7984 to add-a-pair (bounty forbids AI voice/video — must be a real person). (4) SUB-05 Publish the X thread/article introducing the project. (5) Live-URL verification pass — the deferred Phase-7 checks: the wrap cinematic plays on a REAL wrap and only reveals success at the mined tx, is skippable (Esc/Skip), degrades under prefers-reduced-motion; the ambient audio does not autoplay and unlocks/loops on unmute; all /cinematic + /audio media load under COEP require-corp with crossOriginIsolated still true; and the full wrap to decrypt to unwrap loop completes end-to-end. Note which automated gates already passed at phase close (check-types, vitest, build) so the user only runs the human/live items. Per 07-CONTEXT decision F and the floor-it directive.</action>
  <verify>
    <automated>cd /Users/0xemrek/Desktop/fhewrapper && test -f .planning/phases/07-polish-animation-submission/07-UAT.md && grep -q 'SUB-01' .planning/phases/07-polish-animation-submission/07-UAT.md && grep -q 'SUB-04' .planning/phases/07-polish-animation-submission/07-UAT.md && grep -q 'SUB-05' .planning/phases/07-polish-animation-submission/07-UAT.md && grep -qi 'crossOriginIsolated' .planning/phases/07-polish-animation-submission/07-UAT.md</automated>
  </verify>
  <done>07-UAT.md exists capturing SUB-01 (repo public + secret pre-check), SUB-04 (real-person pitch shot list), SUB-05 (X thread), and the deferred live-URL verification pass (cinematic honesty, audio unlock, COEP media, full loop).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private repo to public reveal | Flipping the repo public exposes all tracked files — any committed secret becomes world-readable |
| README to judges | The README is the public entry point; incorrect deploy/isolation guidance would misdirect a judge or a reproducer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09 | Information | secret leak on repo-public (SUB-01) | high | mitigate | 07-UAT.md gates the public flip behind a secret/.env scan pre-check; README documents only env var NAMES (e.g. NEXT_PUBLIC_ALCHEMY_API_KEY), never values |
| T-07-10 | Repudiation | inaccurate README deploy/isolation guidance | low | mitigate | Deploy + COOP/COEP guidance mirrors the verified Phase-1 pipeline (Root Directory packages/nextjs, npm, IPFS build unset, require-corp) and the real next.config.ts |
</threat_model>

<verification>
- Grep gates on README: live URL, 11155111, require-corp, packages/nextjs, add-a-pair present.
- `test -f` + grep gates on 07-UAT.md: SUB-01/SUB-04/SUB-05 + crossOriginIsolated present.
- No automated live check here (docs only); the live items are what 07-UAT.md hands to the user.
</verification>

<success_criteria>
- README rewritten for The Cellar Registry with live URL, network, registry sourcing, add-a-pair, deploy guide, COOP/COEP + SDK-pin notes, and the reusable hooks API (SUB-02).
- 07-UAT.md captures the deferred SUB-01 (repo public), SUB-04 (real-person pitch video), SUB-05 (X thread), and the single live-URL verification pass.
</success_criteria>

<output>
Create `.planning/phases/07-polish-animation-submission/07-04-SUMMARY.md` when done.
</output>
