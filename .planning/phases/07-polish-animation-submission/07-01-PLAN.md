---
phase: 07-polish-animation-submission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/nextjs/lib/cinematicBeats.ts
  - packages/nextjs/lib/cinematicBeats.test.ts
  - packages/nextjs/components/wrap/WrapCinematic.tsx
  - packages/nextjs/components/wrap/WrapPanel.tsx
  - packages/nextjs/components/Preloader.tsx
  - packages/nextjs/public/cinematic/01-fold.mp4
  - packages/nextjs/public/cinematic/02-insert.mp4
  - packages/nextjs/public/cinematic/03-seal.mp4
  - packages/nextjs/public/cinematic/04-age.mp4
  - packages/nextjs/public/cinematic/05-pop.mp4
  - packages/nextjs/public/cinematic/06-token.mp4
  - packages/nextjs/public/cinematic/posters/01-fold.png
  - packages/nextjs/public/cinematic/posters/02-insert.png
  - packages/nextjs/public/cinematic/posters/03-seal.png
  - packages/nextjs/public/cinematic/posters/04-age.png
  - packages/nextjs/public/cinematic/posters/05-pop.png
  - packages/nextjs/public/cinematic/posters/06-token.png
autonomous: true
requirements: [DIF-01, DIF-02]

must_haves:
  truths:
    - "Starting a real wrap opens a full-screen cinematic that advances beat-by-beat as the useWrap stage progresses (approving to wrapping to confirming to done)"
    - "The success/reveal beats (pop, token) never appear before the wrap tx is actually mined (stage === done) — the cinematic never lies about progress"
    - "The cinematic is skippable (Esc key or a Skip control) and skipping closes only the overlay; the real wrap and the honest 4-stage indicator continue underneath"
    - "prefers-reduced-motion suppresses the overlay entirely — the plain WrapStageIndicator carries the flow (DIF-01)"
    - "All cinematic media is self-hosted under packages/nextjs/public/cinematic (same-origin) and each compressed mp4 is small/streamable so it loads under COEP require-corp without breaking crossOriginIsolated (DIF-02)"
  artifacts:
    - "packages/nextjs/lib/cinematicBeats.ts (+ .test.ts) — pure, unit-tested stage to beats engine with the reveal-gate invariant"
    - "packages/nextjs/components/wrap/WrapCinematic.tsx — the tx-driven, skippable, reduced-motion-safe overlay"
    - "packages/nextjs/public/cinematic/*.mp4 (6 compressed beats) + posters/*.png (6 stills)"
  key_links:
    - "WrapPanel.onWrap opens WrapCinematic which reads useWrap.stage; cinematicBeats gates which beat is visible"
    - "cinematicBeats reveal-gate keeps WrapCinematic from showing pop/token until stage === done (honest overlay over the real result)"
---

<objective>
Ship the signature wrap cinematic (DIF-01): a full-screen, engraving-styled overlay that plays six self-hosted video beats (fold, insert, seal, age, pop, token) driven by the REAL Phase-4 `useWrap` transaction lifecycle, skippable at any point, and degrading to the plain stage indicator under `prefers-reduced-motion`. Compress and self-host all six raw beat videos in `public/cinematic/` so they survive the live COOP/COEP `require-corp` isolation (DIF-02). The overlay OVERLAYS the honest 4-stage `WrapStageIndicator` — it never blocks, replaces, or fakes the onchain result; the reveal beats only fire once the wrap tx is actually mined.

Purpose: This is the differentiator wedge to place 1st in the bounty, and the correctness axis is the top judging criterion — so the animation is layered on the verified wrap loop (Phases 2-6, done) and must never misrepresent transaction state.
Output: `lib/cinematicBeats.ts` (+ test), `components/wrap/WrapCinematic.tsx`, 6 compressed mp4s + 6 poster PNGs in `public/cinematic/`, and the wiring in `WrapPanel.tsx` + `Preloader.tsx`.
</objective>

## Phase Goal

**As a** bounty judge, **I want to** watch a signature wrap cinematic that tracks my real transaction (and can be skipped), **so that** the app reads as a polished, trustworthy, first-place submission without ever faking an onchain result.

> Derived from the ROADMAP Phase 7 goal + SC1 (discuss skipped under the floor-it directive; user asleep).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-animation-submission/07-CONTEXT.md

# The wrap flow the cinematic hooks into (stage machine + callbacks):
@packages/nextjs/hooks/useWrap.ts
@packages/nextjs/components/wrap/WrapPanel.tsx
@packages/nextjs/components/wrap/WrapStageIndicator.tsx
# The preloader's Phase-7 video extension point (already documented in-file):
@packages/nextjs/components/Preloader.tsx
# Design treatment — the 6-beat cinematic (narrative titles + tx-stage gating):
@.planning/design/Confidential-Wrapper-Registry.dc.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tested honest beat engine (lib/cinematicBeats.ts)</name>
  <files>packages/nextjs/lib/cinematicBeats.ts, packages/nextjs/lib/cinematicBeats.test.ts</files>
  <behavior>
    - beatsForStage("idle") returns [] (overlay shows nothing)
    - beatsForStage("approving") returns ["fold"]
    - beatsForStage("wrapping") returns ["insert","seal"]
    - beatsForStage("confirming") returns ["age"]
    - beatsForStage("done") returns ["pop","token"]
    - beatsForStage("error") returns [] (overlay yields to the error row below)
    - HONEST REVEAL GATE: the reveal beats "pop" and "token" are absent from beatsForStage(s) for every s that is not "done" (assert explicitly for "confirming" and "wrapping")
    - shouldShowOverlay(stage, reducedMotion): true only when stage is one of approving/wrapping/confirming/done AND reducedMotion is false; false for idle/error and false whenever reducedMotion is true (DIF-01 reduced-motion path)
    - BEAT_MEDIA maps each beat id to its same-origin video path under /cinematic/ and poster under /cinematic/posters/ (assert every path begins with a leading slash and the /cinematic segment — no scheme, no host)
  </behavior>
  <action>Create `lib/cinematicBeats.ts` exporting a pure engine that maps the Phase-4 `WrapStage` (imported as a type from `~~/hooks/useWrap`) to the ordered beat list a tx-driven overlay may play, plus a `shouldShowOverlay(stage, reducedMotion)` predicate and a `BEAT_MEDIA` record. Use the beat ids fold, insert, seal, age, pop, token (matching the six video filenames). The core correctness invariant — the reveal beats are only reachable at `stage === "done"` — is what makes the cinematic honest (it structurally cannot show success before the tx is mined); encode it as the single source of truth the component consumes, and lock it with the tests above. Also export `BEAT_TITLES`/`BEAT_SUB` records carrying the design's narrative copy (Sealing the contract / Into the vessel / Waxed and corked / Aging in the cellar / Uncorked / Transformed) so the component renders labels from data. Keep this file pure (no React, no window, no side effects) so it is fully unit-testable. Write the test file FIRST (RED), watch it fail, then implement (GREEN). Per DIF-01 and the honest-overlay principle in 07-CONTEXT decision A.</action>
  <verify>
    <automated>cd packages/nextjs && npx vitest run lib/cinematicBeats.test.ts</automated>
  </verify>
  <done>cinematicBeats.test.ts passes; the reveal-gate cases prove the reveal beats are unreachable for non-done stages; BEAT_MEDIA paths are all same-origin absolute under /cinematic.</done>
</task>

<task type="auto">
  <name>Task 2: Compress + self-host beats, build the WrapCinematic overlay</name>
  <files>packages/nextjs/public/cinematic/01-fold.mp4, packages/nextjs/public/cinematic/02-insert.mp4, packages/nextjs/public/cinematic/03-seal.mp4, packages/nextjs/public/cinematic/04-age.mp4, packages/nextjs/public/cinematic/05-pop.mp4, packages/nextjs/public/cinematic/06-token.mp4, packages/nextjs/public/cinematic/posters/01-fold.png, packages/nextjs/public/cinematic/posters/02-insert.png, packages/nextjs/public/cinematic/posters/03-seal.png, packages/nextjs/public/cinematic/posters/04-age.png, packages/nextjs/public/cinematic/posters/05-pop.png, packages/nextjs/public/cinematic/posters/06-token.png, packages/nextjs/components/wrap/WrapCinematic.tsx</files>
  <action>First compress the six RAW beat videos in `.planning/design/assets/video/` (01-fold, 02-insert, 03-seal, 04-age, 05-pop, 06-token; ~14-24MB each) with the available ffmpeg 8.0.1 into small streamable H.264 mp4s written to `packages/nextjs/public/cinematic/` — use libx264 CRF around 28, downscale so the longer edge is at most ~720px, strip the audio track (ambient sound is a separate feature in 07-02), and enable faststart so the file streams before full download. Target roughly 1-3MB per beat, and hard-require each output under 6MB. Copy the six matching design storyboard PNGs (03-storyboard-01-fold.png through 08-storyboard-06-token.png in `.planning/design/assets/`) to `public/cinematic/posters/` as 01-fold.png through 06-token.png for the video poster attribute and the reduced-motion still. Then build `components/wrap/WrapCinematic.tsx`: a `"use client"` full-screen overlay (role dialog, dark cellar scrim) that takes the current `WrapStage` as a prop, consumes `cinematicBeats` to pick the visible beat, and renders that beat's video from `BEAT_MEDIA` (muted, playsInline, poster set) with the design's narrative title/subtitle and a 6-dot progress rail. Sequence multi-beat stages (wrapping plays insert then seal; done plays pop then token) via the video onEnded handler; loop the age beat while confirming persists (the variable block-confirmation wait); auto-close shortly after the token reveal video ends. Provide a Skip control and an Escape-key handler that call an onSkip prop (closing ONLY the overlay). Never render the overlay when shouldShowOverlay is false. Do NOT advance to pop/token yourself — the parent passes stage, and the reveal beats only become available when stage === "done" (the reveal gate from Task 1). Use the installed `motion` for enter/exit; no GSAP. Per DIF-01/DIF-02 and 07-CONTEXT decisions A/C.</action>
  <verify>
    <automated>cd packages/nextjs && test $(ls -1 public/cinematic/0*.mp4 | wc -l) -eq 6 && test $(ls -1 public/cinematic/posters/0*.png | wc -l) -eq 6 && test $(find public/cinematic -name '*.mp4' -size +6M | wc -l) -eq 0 && grep -q 'cinematicBeats' components/wrap/WrapCinematic.tsx && npm run build</automated>
  </verify>
  <done>Six streamable mp4s + six posters exist under public/cinematic; every mp4 is under 6MB; WrapCinematic consumes cinematicBeats and renders per-beat video; next build is clean (self-hosted media does not break the build).</done>
</task>

<task type="auto">
  <name>Task 3: Wire the cinematic into the real wrap flow + warm the first beat</name>
  <files>packages/nextjs/components/wrap/WrapPanel.tsx, packages/nextjs/components/Preloader.tsx</files>
  <action>Wire `WrapCinematic` into `WrapPanel.tsx` so it is driven by the REAL `useWrap.stage`. Add local overlay state (open on wrap submit, closed on skip/Esc, closed on error, closed once the reveal auto-dismisses). In `onWrap`, open the overlay only when motion is allowed — read `prefers-reduced-motion` (use `useReducedMotion` from motion/react, already used by Preloader) and skip opening the overlay when reduced (the existing WrapStageIndicator carries the flow). Pass the live `stage` from `useWrap` into `WrapCinematic` and an `onSkip` that closes only the overlay. CRITICAL honesty constraints (DIF-01, top judging axis): (1) the overlay OVERLAYS — do not remove or hide the existing `WrapStageIndicator`, the inline `ExplorerTxLink`, the error row, or the on-done `PairCardDecrypt` proof; they remain the source of truth and keep rendering beneath. (2) Skipping/closing the overlay must NOT touch the `useWrap` mutation — the real approve/wrap tx keeps running and its result is unchanged. (3) The overlay reflects `stage`; it never drives it. On `stage === "error"` close the overlay so the typed error row is visible. Then extend `Preloader.tsx` at its documented Phase-7 extension point: implement the reserved `case "video"` branch in `preloadAsset` (create the video element, set preload metadata, resolve on canplaythrough or error — never reject) and append ONLY the first beat `{ kind: "video", src: "/cinematic/01-fold.mp4" }` to `CRITICAL_ASSETS` so the opening beat is warm without gating the whole cover on all six videos. Do not add audio here (07-02 owns audio; it cannot autoplay anyway). Per DIF-01 and 07-CONTEXT decision A.</action>
  <verify>
    <automated>cd packages/nextjs && grep -q 'WrapCinematic' components/wrap/WrapPanel.tsx && grep -q 'WrapStageIndicator' components/wrap/WrapPanel.tsx && grep -q '/cinematic/01-fold.mp4' components/Preloader.tsx && npx vitest run && npm run build</automated>
  </verify>
  <done>WrapPanel renders WrapCinematic driven by useWrap.stage AND still renders the honest WrapStageIndicator + error row + on-done decrypt proof; Preloader warms the first beat via the video branch; full vitest suite green; next build emits /wrap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| onchain wrap tx to cinematic overlay | The overlay renders a narrative over a real, wallet/relayer-owned transaction — it must reflect, never fabricate, onchain state |
| self-hosted media to COEP require-corp runtime | Media served to a cross-origin-isolated document; a cross-origin asset would silently drop crossOriginIsolated and break the FHE WASM worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Repudiation/Integrity | WrapCinematic + cinematicBeats | high | mitigate | Reveal beats (pop/token) structurally gated to stage === done via cinematicBeats (unit-tested reveal-gate); overlay never replaces the honest WrapStageIndicator; skip does not touch the useWrap mutation |
| T-07-02 | Denial of Service | public/cinematic/*.mp4 bundle weight | medium | mitigate | ffmpeg compress each beat to under 6MB streamable H.264 + faststart; poster fallback; reduced-motion path skips the overlay; Preloader warms only the first beat |
| T-07-03 | Tampering | cross-origin isolation break from media | high | mitigate | All beats + posters self-hosted under /public (same-origin), no CDN/hotlink; BEAT_MEDIA paths asserted scheme-less absolute; next build must stay clean; live crossOriginIsolated re-checked in 07-UAT |
| T-07-04 | Information | video src injection | low | accept | Beat sources are compile-time constants in BEAT_MEDIA, never user input — no untrusted path reaches the video element |
</threat_model>

<verification>
- `npx vitest run lib/cinematicBeats.test.ts` — reveal-gate + overlay-visibility invariants (RED-first).
- Full `npx vitest run` — no regression across the 112 existing tests.
- `npm run build` — `/wrap` emitted; self-hosted `/cinematic` media does not break the build.
- Grep gates: WrapPanel imports WrapCinematic AND still renders WrapStageIndicator; Preloader references `/cinematic/01-fold.mp4`.
- Deferred to 07-UAT (live URL): cinematic plays on a real wrap, reveal only at mined tx, Skip/Esc works, reduced-motion falls back to the stage indicator, and `crossOriginIsolated === true` is preserved with the media loaded.
</verification>

<success_criteria>
- A real wrap opens the cinematic; beats track `useWrap.stage`; pop/token only after the tx is mined.
- Overlay is skippable (Esc + Skip) and skipping never affects the real tx; reduced-motion suppresses the overlay.
- Six compressed mp4s + six posters live in `public/cinematic/`, each mp4 under 6MB, same-origin.
- Full vitest suite + `next build` green; the honest WrapStageIndicator + decrypt proof still render.
</success_criteria>

<output>
Create `.planning/phases/07-polish-animation-submission/07-01-SUMMARY.md` when done.
</output>
