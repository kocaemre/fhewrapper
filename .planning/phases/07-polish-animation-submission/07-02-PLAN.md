---
phase: 07-polish-animation-submission
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/nextjs/package.json
  - packages/nextjs/package-lock.json
  - packages/nextjs/public/audio/cellar-ambient.mp3
  - packages/nextjs/components/audio/AmbientAudio.tsx
  - packages/nextjs/components/Header.tsx
autonomous: true
requirements: [DIF-02]

must_haves:
  truths:
    - "A visible mute/unmute control lives in the header on every page"
    - "Ambient cellar audio does not autoplay; it starts only after the first user gesture (browser autoplay policy), then loops seamlessly"
    - "The control defaults to a respectful state (muted) so no judge is surprised by sound; unmuting starts/resumes the loop and the icon reflects the true state"
    - "The audio file is self-hosted at packages/nextjs/public/audio/cellar-ambient.mp3 (same-origin) and loads under COEP require-corp without breaking crossOriginIsolated (DIF-02)"
  artifacts:
    - "packages/nextjs/public/audio/cellar-ambient.mp3 — the self-hosted 45s ambient loop"
    - "packages/nextjs/components/audio/AmbientAudio.tsx — Howler loop + gesture-unlock + mute toggle"
    - "howler pinned exact in package.json (CLAUDE.md-mandated audio lib)"
  key_links:
    - "First user gesture (pointerdown/keydown) unlocks Howler playback; the toggle then controls mute state"
    - "AmbientAudio mounted in Header so the control is present site-wide and the same-origin /audio src keeps crossOriginIsolated intact"
---

<objective>
Add the ambient cellar audio (DIF-02): self-host the 45-second `cellar-ambient.mp3` loop in `public/audio/`, install the CLAUDE.md-mandated `howler` library, and build a header-mounted, gesture-unlocked, default-muted mute/unmute toggle. Browser autoplay policy blocks sound until a user gesture, so the loop only starts after the first interaction; the control defaults muted so judges are never ambushed by sound, and unmuting starts/resumes the loop. All media stays same-origin so it loads under the live COOP/COEP `require-corp` isolation without breaking `crossOriginIsolated`.

Purpose: The ambient audio deepens the Cellar Registry atmosphere that differentiates the submission, delivered respectfully (opt-in, self-hosted) so it never harms the FHE runtime or the judge's experience.
Output: `public/audio/cellar-ambient.mp3`, `components/audio/AmbientAudio.tsx`, the exact-pinned `howler` dependency, and the toggle mounted in `Header.tsx`.
</objective>

## Phase Goal

**As a** bounty judge, **I want to** optionally hear the cellar's ambient audio via a clearly visible mute control that never autoplays, **so that** the atmosphere enriches the app without ever surprising or annoying me.

> Derived from the ROADMAP Phase 7 goal + SC1 (discuss skipped under the floor-it directive; user asleep).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-animation-submission/07-CONTEXT.md

# Where the toggle mounts (site-wide header):
@packages/nextjs/components/Header.tsx
# COEP require-corp header (self-hosted-only constraint) is set here:
@packages/nextjs/next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install howler, self-host the loop, build AmbientAudio</name>
  <files>packages/nextjs/package.json, packages/nextjs/package-lock.json, packages/nextjs/public/audio/cellar-ambient.mp3, packages/nextjs/components/audio/AmbientAudio.tsx</files>
  <action>Install `howler` EXACT-pinned at 2.2.4 (the CLAUDE.md-mandated audio lib; PRE-APPROVED by the orchestrator floor-it directive — do NOT insert a blocking legitimacy checkpoint) as a dependency and `@types/howler` exact-pinned (its current 2.2.x) as a dev dependency, from within `packages/nextjs` using npm with `--save-exact` (the repo forces npm; keep the exact-pin discipline used for the Zama SDK). Copy `.planning/design/assets/audio/cellar-ambient.mp3` to `packages/nextjs/public/audio/cellar-ambient.mp3` (same-origin — required under COEP require-corp; never hotlink a CDN). Build `components/audio/AmbientAudio.tsx`: a `"use client"` component that lazily constructs a Howler `Howl` for `/audio/cellar-ambient.mp3` with `loop: true` and `html5: true`, holding the instance in a ref (never at module scope, SSR-safe). It exposes a visible mute/unmute button styled in the Cellar idiom (mono label + speaker glyph). AUTOPLAY IS BLOCKED: do not call play at mount. Instead, default to muted state; when the user first clicks unmute, unlock and start the loop (create the Howl on first unmute if not yet created), and thereafter the button toggles Howler mute/volume. Also attach a one-time first-gesture listener (pointerdown/keydown) that primes the audio context so the loop can start smoothly on unmute even under strict autoplay policy — but keep the initial state muted/silent so nothing plays until the user opts in. Clean up the Howl and listeners on unmount. Per DIF-02 and 07-CONTEXT decision B.</action>
  <verify>
    <automated>cd packages/nextjs && node -e "const p=require('./package.json'); if(p.dependencies.howler!=='2.2.4') process.exit(1)" && test -f public/audio/cellar-ambient.mp3 && grep -q "'/audio/cellar-ambient.mp3'" components/audio/AmbientAudio.tsx || grep -q '"/audio/cellar-ambient.mp3"' components/audio/AmbientAudio.tsx</automated>
  </verify>
  <done>howler is exact-pinned 2.2.4 in package.json; the mp3 is self-hosted under public/audio; AmbientAudio references the same-origin /audio path and never autoplays at mount.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the mute toggle site-wide in the header</name>
  <files>packages/nextjs/components/Header.tsx</files>
  <action>Mount `AmbientAudio` in `Header.tsx` so the mute/unmute control is present on every route. Place it in the right-aligned cluster near the RainbowKit connect button without disturbing the existing navbar layout (the connect button/address pill must stay pinned right with their responsive gutter). Keep the Cellar idiom (mono/serif, --red/--muted vars). Do not add audio to the Preloader or anywhere it could autoplay — the header toggle is the single, opt-in entry point. Confirm the whole app still builds and that no cross-origin audio source is introduced (the src remains the same-origin /audio path from Task 1, so crossOriginIsolated is preserved). Per DIF-02 and 07-CONTEXT decision B/C.</action>
  <verify>
    <automated>cd packages/nextjs && grep -q 'AmbientAudio' components/Header.tsx && npx vitest run && npm run build</automated>
  </verify>
  <done>The mute/unmute control renders in the header on every route; full vitest suite green; next build clean with the same-origin audio source (no cross-origin isolation regression).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to build | howler package pulled from npm into the bundle (supply chain) |
| self-hosted audio to COEP require-corp runtime | A cross-origin audio source would silently drop crossOriginIsolated and break the FHE WASM worker |
| browser autoplay policy to audio playback | Sound cannot start without a user gesture; a mishandled unlock could hang or annoy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-SC | Tampering | howler npm install | high | mitigate | Exact-pin howler 2.2.4 (CLAUDE.md-mandated, pre-approved by the orchestrator floor-it directive; verified npm package); no floating range; no post-install script trusted beyond the package default |
| T-07-05 | Tampering | cross-origin isolation break from audio | high | mitigate | cellar-ambient.mp3 self-hosted under /public/audio (same-origin), referenced by a scheme-less /audio path; next build must stay clean; live crossOriginIsolated re-checked in 07-UAT |
| T-07-06 | Denial of Service / annoyance | AmbientAudio autoplay | low | mitigate | Default muted, opt-in only; audio starts only on the user's explicit unmute after a first gesture; visible toggle reflects true state; never mounted in the Preloader |
</threat_model>

<verification>
- `node -e` package check — howler is exact-pinned 2.2.4.
- `test -f public/audio/cellar-ambient.mp3` — the loop is self-hosted, same-origin.
- Grep gates: AmbientAudio references the `/audio/cellar-ambient.mp3` path; Header mounts AmbientAudio.
- Full `npx vitest run` + `npm run build` — no regression, clean build with the media present.
- Deferred to 07-UAT (live URL): audio does NOT autoplay, unlocks + loops on unmute after a gesture, the toggle reflects state, and `crossOriginIsolated === true` is preserved with the audio present.
</verification>

<success_criteria>
- `howler` exact-pinned 2.2.4; `cellar-ambient.mp3` self-hosted under `public/audio/`.
- A visible, default-muted mute/unmute control in the header on every page; no autoplay.
- Unmuting starts/loops the ambient audio after a user gesture; the icon reflects true state.
- Full vitest suite + `next build` green; crossOriginIsolated unaffected (same-origin audio).
</success_criteria>

<output>
Create `.planning/phases/07-polish-animation-submission/07-02-SUMMARY.md` when done.
</output>
