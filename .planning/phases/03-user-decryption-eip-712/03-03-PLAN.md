---
phase: 03-user-decryption-eip-712
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - packages/nextjs/components/decrypt/DecryptQuickPicks.tsx
  - packages/nextjs/components/decrypt/PermitIndicator.tsx
  - packages/nextjs/components/decrypt/DecryptPanel.tsx
  - packages/nextjs/components/Header.tsx
  - packages/nextjs/public/02-bottle-hero.png
autonomous: false
requirements: [DEC-01, DEC-02, DEC-03, DEC-04]

must_haves:
  truths:
    - "The header exposes a Decrypt nav link; /decrypt shows registry cToken quick-pick chips (the registry-token entry point) and a VIEWING KEY ACTIVE badge once a permit is cached (DEC-02/DEC-03)."
    - "A real decrypt — registry card AND a pasted out-of-registry ERC-7984 — succeeds on the live URL with a single signature reused across decrypts (DEC-01/DEC-02/DEC-03)."
    - "A no-ACL token shows 'NO DECRYPTION ACCESS' and a zero balance shows 0 on the live URL, never a hang (DEC-04)."
  artifacts:
    - packages/nextjs/components/decrypt/DecryptQuickPicks.tsx
    - packages/nextjs/components/decrypt/PermitIndicator.tsx
    - packages/nextjs/public/02-bottle-hero.png
  key_links:
    - "DecryptQuickPicks sources chips from Phase-2 useRegistryPairs (valid only) and sets the panel address; PermitIndicator reads useIsAllowed for the cached-permit badge."
    - "Header Decrypt link routes to /decrypt; hero art is self-hosted under public/ so it survives COEP require-corp."
---

## Phase Goal

**As a** judge or developer evaluating the app, **I want to** decrypt my confidential (ERC-7984) balance for any token via a correct one-signature EIP-712 flow, **so that** I can see and trust the real cleartext result of every wrap and unwrap.

<objective>
Finish the decrypt feature and prove it on the live URL. Adds the registry-token quick-pick entry point (DEC-02 "both entry points"), the cached-permit VIEWING KEY ACTIVE indicator (DEC-03 visible), the self-hosted bottle-hero illustration, and the Decrypt header nav — then gates the phase on a REAL end-to-end decrypt verified on the deployed Vercel URL (relayer under require-corp), not just localhost.

Purpose: The EIP-712 user-decryption is relayer-dependent and cannot be proven by unit tests alone (RESEARCH Open Questions 1/3). The live-URL check is the phase gate that closes DEC-01…DEC-04.
Output: Two new decrypt components, the wired-up panel + header, the self-hosted asset, and a passing live UAT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-user-decryption-eip-712/03-RESEARCH.md
@.planning/phases/03-user-decryption-eip-712/03-UI-SPEC.md
@.planning/phases/03-user-decryption-eip-712/03-CONTEXT.md
@packages/nextjs/components/Header.tsx
@packages/nextjs/hooks/useRegistryPairs.ts
@packages/nextjs/lib/tokenSymbol.ts

# From 03-02 (Wave 2) — the panel this plan enriches:
@packages/nextjs/components/decrypt/DecryptPanel.tsx

# Self-hosted asset source (copy into public/ — COEP require-corp blocks cross-origin media):
# .planning/design/assets/02-bottle-hero.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quick-picks + permit indicator + self-hosted hero + Decrypt nav</name>
  <files>packages/nextjs/components/decrypt/DecryptQuickPicks.tsx, packages/nextjs/components/decrypt/PermitIndicator.tsx, packages/nextjs/components/decrypt/DecryptPanel.tsx, packages/nextjs/components/Header.tsx, packages/nextjs/public/02-bottle-hero.png</files>
  <action>Enrich the decrypt panel and make it discoverable.

Copy the asset: copy `.planning/design/assets/02-bottle-hero.png` to `packages/nextjs/public/02-bottle-hero.png` (self-hosted → same-origin → survives COEP require-corp; DIF-02 pattern from Phase 2). If it exceeds ~1.6MB, downscale to ≤1152px via sips as Phase 2 did for the hero.

DecryptQuickPicks.tsx ("use client"): props { onPick: (address: Address) => void; selected?: Address }. Read Phase-2 useRegistryPairs(); render the VALID pairs' confidential tokens as mono chips "◈ {cSymbol}" (cSymbol via normalizeSymbol(pair.confidential.symbol); padding 7px 13px, JetBrains Mono 12px/600, flex-wrap with a "or pick:" italic --faint hint). Clicking a chip calls onPick(pair.confidential.address); the selected chip gets a --blue border + --blue-dim fill. This is the registry-token entry point that, alongside the paste input, proves "any token" (CONTEXT decision B).

PermitIndicator.tsx ("use client"): props { address?: Address }. Render a small green mono badge "◈ VIEWING KEY ACTIVE" (JetBrains Mono 10.5px/700, uppercase, letter-spacing 0.14em, --green) ONLY when useIsAllowed({ contractAddresses: address ? [address] : [] }).data === true (verified 3.0.0 hook). Hidden otherwise. An expiry suffix ("· expires …") is optional — include only if a reliable relative time is derivable; otherwise show the badge alone (DEC-03 permit-status indicator; Claude's discretion per CONTEXT).

Wire into DecryptPanel.tsx (modify): in the left illustration column replace the placeholder with an <img src="/02-bottle-hero.png"> (object-fit cover, min-height 420px, decorative alt "Engraved bottle, wax-sealed, with a scroll inside"). In the right column, add DecryptQuickPicks below the address input (its onPick sets the panel address + resets to idle; pass the current address as selected) and PermitIndicator next to the CTA (pass the resolved address). Keep all 03-02 panel logic intact.

Header.tsx (modify): add a "Decrypt" nav link to /decrypt using next/link, styled in the engraving idiom (Gelasio; active route → --red underline via usePathname). Preserve the existing right-aligned RainbowKit connect cluster; the link sits in the navbar-start / left region so the connect button stays right-aligned.</action>
  <verify>
    <automated>cd packages/nextjs && test -f public/02-bottle-hero.png && npm run check-types && npm run lint && npx vitest run && npm run build</automated>
  </verify>
  <done>The hero asset is self-hosted under public/; typecheck, lint, vitest, and next build exit 0. /decrypt shows registry quick-pick chips (valid pairs), the VIEWING KEY ACTIVE badge appears once a permit is cached, and the header has a working Decrypt link. No new aesthetic — Phase-2 tokens/vars only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-URL decrypt verification (phase gate — DEC-01…DEC-04)</name>
  <action>Deploy the latest commit to the live URL and run the human verification steps below against the REAL relayer (localhost is insufficient — this is the phase gate per RESEARCH Open Questions 1/3). This is a blocking human-verify checkpoint: do not mark the phase complete until DEC-01…DEC-04 all pass on the deployed URL. Record the concrete no-ACL error class observed (resolves RESEARCH Open Question 1) in the plan summary.</action>
  <what-built>The complete Phase-3 decrypt feature: per-card blur→reveal decrypt on registry PairCards (03-01), the /decrypt paste-an-address panel with ERC-165 validation + typed error states (03-02), registry quick-picks, the cached-permit VIEWING KEY ACTIVE indicator, self-hosted hero art, and a Decrypt nav link (this plan). All driven by the shared useUserDecrypt engine + a single reusable EIP-712 permit on the exact-pinned 3.0.0 SDK.</what-built>
  <how-to-verify>
    Deploy the latest commit to the live URL, then verify the REAL relayer path (this cannot be proven on localhost alone — the phase gate per RESEARCH Open Questions 1/3):

    1. Deploy: push the phase branch/commits so Vercel auto-deploys (the Phase-1 GitHub→Vercel pipeline). Open https://fhewrapper-nextjs.vercel.app/ and confirm in the browser console that `crossOriginIsolated === true` (isolation failures are silent until you decrypt).
    2. Connect MetaMask on Sepolia (use the existing ChainGuard/connect flow). Use a wallet that HOLDS at least one registry cToken (claim via the Zama faucet if needed).
    3. DEC-01: on a registry PairCard for a cToken you hold, click Decrypt → approve ONE EIP-712 signature → the blurred ciphertext reveals the correct cleartext balance (spot-check magnitude against what you hold; decimals must look right).
    4. DEC-03: click Decrypt on a SECOND card → NO second signature prompt (the permit is cached in-window). Open /decrypt — the "◈ VIEWING KEY ACTIVE" badge is shown.
    5. DEC-02: on /decrypt, paste an out-of-registry ERC-7984 address the wallet holds → Sign & Decrypt → cleartext reveals. Then paste a non-ERC-7984 address (e.g. a plain ERC-20) → "NOT A CONFIDENTIAL (ERC-7984) TOKEN"; paste garbage → "NOT A VALID ADDRESS". Try a registry quick-pick chip → it fills the address and decrypts.
    6. DEC-04: paste an ERC-7984 the wallet has NO ACL access to → "NO DECRYPTION ACCESS" appears (graceful, no infinite spinner). A token with a zero/never-initialized balance → reveals "0 {symbol}" in the green DECRYPTED BALANCE state.
    7. Theme + reduced-motion: toggle parchment/cellar (colors follow); with OS reduced-motion on, blur→reveal shows final states without animation.

    Confirm all six requirement checks pass on the LIVE URL. Note the concrete error class observed for the no-ACL case (resolves RESEARCH Open Question 1) in the summary.
  </how-to-verify>
  <resume-signal>Type "approved" once DEC-01…DEC-04 all verify on the live URL, or describe which step failed (e.g. relayer error, wrong decimals, hung spinner) so it can be fixed before phase sign-off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → wallet (MetaMask) | EIP-712 permit signature; the SDK builds the canonical typed data. |
| client → Zama relayer/gateway | Live decrypt under COEP require-corp on the deployed URL. |
| self-hosted asset → browser | Hero PNG served same-origin under require-corp. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Spoofing | EIP-712 prompt (useAllow via useUserDecrypt) | medium | mitigate | The SDK assembles the canonical UserDecryptRequestVerification typed data; the app never constructs arbitrary sign requests. Footer copy + PermitIndicator explain what the signature grants (a re-encryption key scoped to the wallet). |
| T-03-06 | Denial of Service (UX) | live relayer under require-corp | medium | mitigate | Phase-gate checkpoint verifies a real decrypt on the deployed URL (crossOriginIsolated === true); the 4-stage machine bounds the wait and surfaces a typed error, never a silent hang (DEC-04). |
| T-03-01 | Information Disclosure | live revealed cleartext | medium | mitigate | Cleartext stays client-side/in-session; nothing is broadcast on-chain or sent to a backend (none exists). |
| T-03-SC | Tampering | npm installs / self-hosted asset | low | accept | No package installs; the only new artifact is a same-origin PNG copied from the vetted design assets (no cross-origin media under require-corp). |
</threat_model>

<verification>
- `cd packages/nextjs && test -f public/02-bottle-hero.png` — asset self-hosted.
- `npm run check-types && npm run lint && npx vitest run && npm run build` — all exit 0.
- Blocking human-verify checkpoint PASSES on https://fhewrapper-nextjs.vercel.app/ covering DEC-01, DEC-02, DEC-03, DEC-04 against the real relayer.
</verification>

<success_criteria>
- Quick-pick chips (registry entry point), the cached-permit VIEWING KEY ACTIVE indicator, the self-hosted hero, and the Decrypt nav link are live and clean (typecheck/lint/vitest/build).
- A judge can decrypt a registry cToken balance AND a pasted out-of-registry ERC-7984 on the live URL, with one reused signature.
- No-ACL shows a graceful message and zero balance shows 0 — never a spinner — on the live URL.
- The phase gate (real decrypt on the deployed URL) is approved.
</success_criteria>

<output>
Create `.planning/phases/03-user-decryption-eip-712/03-03-SUMMARY.md` when done.
</output>
