---
phase: 02-registry-browse
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - README.md
autonomous: true
requirements: [REG-05, REG-07]
user_setup: []

must_haves:
  truths:
    - "The README documents adding a custom/dev wrapper pair via registry/pairs.config.ts with a concrete, copy-pasteable example"
    - "The README explains that pairs are deduped by ERC-7984 (confidential) address and that the onchain registry entry wins on conflict"
    - "The README states the registry is sourced from the onchain Sepolia registry (never hardcoded) and names the supported network"
  artifacts:
    - README.md
  key_links:
    - "The README example matches the LocalPair type shape exported by registry/types.ts / registry/pairs.config.ts (from 02-01)"
---

<objective>
Document the hybrid add-a-pair mechanism (REG-07): how a developer overlays a custom or dev-only wrapper pair through `packages/nextjs/registry/pairs.config.ts`, how the merge dedupes by confidential-token address with the onchain registry winning on conflict (REG-05), and how the registry is sourced onchain.

Purpose: REG-07 requires the README to document the add-a-pair process with a concrete example; REG-05's hybrid-sourcing contract must be explained so a judge/developer can verify it. This is a deliverable, not app behavior; the full submission README (live URL, deploy scripts) is Phase 7 (SUB-02).
Output: An "Adding a wrapper pair" section in the root README with a real `LocalPair` example and the dedup/precedence rules, plus a short "How the registry is sourced" note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-registry-browse/02-RESEARCH.md
@.planning/phases/02-registry-browse/02-CONTEXT.md
@README.md

# Shape to document (created in 02-01)
@packages/nextjs/registry/pairs.config.ts
@packages/nextjs/registry/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: README "Adding a wrapper pair" + registry sourcing section</name>
  <files>README.md</files>
  <action>Add a section to the root `README.md` documenting REG-05/REG-07. Include: (1) a short "How the registry is sourced" note stating pairs are read live from the onchain Sepolia Wrappers Registry at 0x2f0750Bbb0A246059d80e94c454586a7F27a128e (never hardcoded), revoked pairs flagged via isValid, and token metadata resolved via multicall; (2) an "Adding a wrapper pair" subsection explaining that `packages/nextjs/registry/pairs.config.ts` exports a typed `localPairs: LocalPair[]` overlay, with a concrete copy-pasteable example entry (tokenAddress = an underlying ERC-20, confidentialTokenAddress = its ERC-7984 wrapper, optional isValid and metadata overrides) that matches the `LocalPair` type from `registry/types.ts`; (3) the precedence rule in plain terms — entries are deduped by the lowercased confidential (ERC-7984) address and the ONCHAIN registry entry wins when the same confidential address exists both onchain and in the local config (REG-05 / REG SC4). Name the supported network (Sepolia testnet). Keep the example addresses clearly illustrative (do not inject a fake pair into the shipped app — the config file itself stays empty/commented, per 02-01). Ensure the documented type shape stays consistent with the actual exported `LocalPair`.</action>
  <verify>
    <automated>grep -q "pairs.config.ts" README.md && grep -qi "onchain wins\|onchain registry entry wins\|onchain.*win" README.md && grep -qi "add" README.md</automated>
  </verify>
  <done>README has a documented add-a-pair section with a concrete `LocalPair` example, states dedup-by-confidential-address with onchain winning on conflict, and notes onchain sourcing + the Sepolia network; the example matches the real exported type.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> developer | README instructs how to add local config entries (no runtime input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | documented local-config example could paste an unchecked address | low | mitigate | README notes addresses are validated by viem isAddress/getAddress at load (from 02-01) and that onchain entries win, so a bad/duplicate local entry cannot override a real onchain pair |
</threat_model>

<verification>
- `grep` confirms the README references `pairs.config.ts`, the onchain-wins precedence, and the add-a-pair flow.
- The documented `LocalPair` example fields match the exported type from `registry/types.ts`.
</verification>

<success_criteria>
REG-07 (README documents the add-a-pair process with a concrete example) and the documentation half of REG-05 (hybrid sourcing, onchain wins on conflict) are satisfied.
</success_criteria>

<output>
Create `.planning/phases/02-registry-browse/02-04-SUMMARY.md` when done.
</output>
