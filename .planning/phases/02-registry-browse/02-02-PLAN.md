---
phase: 02-registry-browse
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - packages/nextjs/public/icons/cusdc.png
  - packages/nextjs/public/icons/cusdt.png
  - packages/nextjs/public/icons/cweth.png
  - packages/nextjs/public/icons/cbron.png
  - packages/nextjs/public/icons/czama.png
  - packages/nextjs/public/icons/ctgbp.png
  - packages/nextjs/public/icons/cxaut.png
  - packages/nextjs/public/01-hero.png
  - packages/nextjs/app/layout.tsx
  - packages/nextjs/styles/globals.css
  - packages/nextjs/components/registry/TokenIcon.tsx
  - packages/nextjs/components/registry/PairBadge.tsx
  - packages/nextjs/components/registry/AddressCopyButton.tsx
  - packages/nextjs/components/registry/RegistryHero.tsx
  - packages/nextjs/components/registry/PairCard.tsx
  - packages/nextjs/app/page.tsx
autonomous: true
requirements: [REG-04, REG-06]
user_setup: []

must_haves:
  truths:
    - "The 7 branded cTokens display their self-hosted engraving icons; any pair without a matching icon shows a monogram tile (never a broken image)"
    - "Each pair card shows both ERC-20 and ERC-7984 addresses (copyable, with a success toast), symbol/name/decimals, and a valid/revoked badge in the Cellar Registry engraving theme"
    - "The app renders in Gelasio + JetBrains Mono, with parchment (default) and cellar themes driven entirely by CSS custom properties"
  artifacts:
    - packages/nextjs/public/icons/cusdc.png
    - packages/nextjs/components/registry/TokenIcon.tsx
    - packages/nextjs/components/registry/AddressCopyButton.tsx
    - packages/nextjs/components/registry/RegistryHero.tsx
    - packages/nextjs/components/registry/PairBadge.tsx
    - packages/nextjs/styles/globals.css
    - packages/nextjs/app/layout.tsx
  key_links:
    - "TokenIcon resolves iconFor(confidentialSymbol) -> /icons/c{key}.png and falls back to a monogram tile on img error"
    - "RegistryHero pairCount binds to the live valid-pair count from useRegistryPairs"
    - "engraving components read --bg/--ink/--panel/--blue/--green/--red CSS vars scoped to [data-theme]"
---

<objective>
Dress the working registry slice from 02-01 in the locked "Cellar Registry" engraving design: swap fonts to Gelasio + JetBrains Mono, add the parchment/cellar CSS-variable themes, self-host and web-optimize the 7 real cToken icons + the hero art, and build the full engraving `PairCard` (icon, copyable both-network addresses, valid/revoked badge, decimals, Wrap CTA) plus the hero banner.

Purpose: REG-04 (the 7 branded cTokens surface with their real icons) and REG-06 (each card shows both addresses + metadata + status) at the presentation layer, faithful to `.planning/design/Confidential-Wrapper-Registry.dc.html` (CONTEXT decision B, 02-UI-SPEC.md). Icons are self-hosted and resized to protect bundle size + COEP require-corp (CONTEXT decision B, DIF-02).
Output: Themed fonts + CSS vars, 7 optimized icons + hero in public/, and the engraving `TokenIcon`/`PairBadge`/`AddressCopyButton`/`RegistryHero`/full `PairCard` components wired into the page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-registry-browse/02-UI-SPEC.md
@.planning/phases/02-registry-browse/02-CONTEXT.md
@.planning/design/Confidential-Wrapper-Registry.dc.html

# Built in 02-01 (this plan upgrades PairCard + page, reuses the hook + lib helpers)
@packages/nextjs/app/page.tsx
@packages/nextjs/components/registry/PairCard.tsx
@packages/nextjs/app/layout.tsx
@packages/nextjs/styles/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Self-host + optimize assets; swap fonts; add parchment/cellar themes</name>
  <files>packages/nextjs/public/icons/cusdc.png, packages/nextjs/public/icons/cusdt.png, packages/nextjs/public/icons/cweth.png, packages/nextjs/public/icons/cbron.png, packages/nextjs/public/icons/czama.png, packages/nextjs/public/icons/ctgbp.png, packages/nextjs/public/icons/cxaut.png, packages/nextjs/public/01-hero.png, packages/nextjs/app/layout.tsx, packages/nextjs/styles/globals.css</files>
  <action>Copy the 7 icons from `.planning/design/assets/icons/` into `packages/nextjs/public/icons/` and RESIZE/COMPRESS them first — the sources are 1024px-square ~2MB each and must not ship at that size (bundle + COEP, CONTEXT decision B). Use `sips -Z 128` (or `cwebp` if keeping WebP output) to produce ~96-128px web-optimized files targeting well under 200KB each; keep the design filenames `cusdc.png`, `cusdt.png`, `cweth.png`, `cbron.png`, `czama.png`, `ctgbp.png`, `cxaut.png`. Also copy `.planning/design/assets/01-hero.png` (~9.9MB source) into `packages/nextjs/public/01-hero.png` resized to a max width around 1600px and compressed. During this task, log each of the 7 confidential symbols through `iconKey` (from 02-01) and confirm the normalized key matches the intended filename; if any of the 7 does not resolve exactly, add an explicit address-to-icon override map in `lib/tokenSymbol.ts` so all 7 branded cTokens resolve (RESEARCH Open Question 2). In `app/layout.tsx`, replace the Telegraf `<link>` with the Google Fonts `<link>` for `Gelasio` (ital 400/500/600/700) + `JetBrains Mono` (400/500/600/700) per UI-SPEC. In `styles/globals.css`, add the parchment and cellar CSS-variable blocks transcribed verbatim from the `.dc.html` head (the full `--bg/--bg2/--panel/--panel2/--ink/--muted/--faint/--line/--line-soft/--line-faint/--red/--red-dim/--blue/--blue-dim/--blue-line/--green/--green-dim/--block/--block-fg/--shadow` sets and the `inkPulse`/`toastIn` keyframes) and set the page body font stack to Gelasio + JetBrains Mono. Map these onto the existing next-themes mechanism: parchment is the default (light) theme and cellar is the dark theme, so the engraving vars resolve from the active `data-theme` without breaking the Phase-1 theme toggle or RainbowKit theming. Keep all media same-origin (self-hosted) — do not reference any CDN image (DIF-02, COEP require-corp).</action>
  <verify>
    <automated>cd packages/nextjs && test $(ls public/icons/*.png | wc -l | tr -d ' ') -eq 7 && test -f public/01-hero.png && [ -z "$(find public/icons -name '*.png' -size +200k)" ] && npm run build</automated>
  </verify>
  <done>All 7 icons + the hero exist under public/, each icon is under 200KB, the hero is compressed; fonts are Gelasio + JetBrains Mono; parchment/cellar CSS vars resolve on the active theme; `next build` succeeds; every branded cToken symbol resolves to an existing icon file (override map added if needed).</done>
</task>

<task type="auto">
  <name>Task 2: Engraving atoms — TokenIcon, PairBadge, AddressCopyButton, RegistryHero</name>
  <files>packages/nextjs/components/registry/TokenIcon.tsx, packages/nextjs/components/registry/PairBadge.tsx, packages/nextjs/components/registry/AddressCopyButton.tsx, packages/nextjs/components/registry/RegistryHero.tsx</files>
  <action>Build the four presentation atoms per 02-UI-SPEC Component Inventory, styling via the CSS vars (no default daisyUI look). `TokenIcon`: 46x46 self-hosted `<img>` (2px `--line` border, border-radius 99px, object-fit cover, decorative empty alt) resolving `iconFor(confidentialSymbol)`; on img error render the monogram fallback tile (first letter of the confidential symbol, `--panel2` fill, `--line` border, same 46px rounded frame) so a missing icon never shows a broken image (UI-SPEC Icon Mapping fallback). `PairBadge`: valid/revoked pill, 1.5px border in `--green`/`--red`, matching text color, JetBrains Mono uppercase, copy `✓ Valid` / `✕ Revoked`. `AddressCopyButton`: truncated address via `truncateAddress` (0x + first4 + ellipsis + last4) with a trailing copy glyph; click calls `navigator.clipboard.writeText(fullAddress)` then fires a react-hot-toast success toast (title `Copied`, body naming the side + which standard, per UI-SPEC Copywriting); an `variant="confidential"` uses the blue border/fill (`--blue-line` / `--blue-dim`). `RegistryHero`: the banner using self-hosted `/01-hero.png` with the overlay copy transcribed from UI-SPEC (eyebrow, headline + em, body, stats), 2px `--line` border and hard `--shadow`, overlay padding `5% 52% 5% 6.5%`, and a `pairCount` prop bound by the caller to the live valid-pair count. Render token metadata strings through normal JSX escaping only (T-02-01) — never dangerouslySetInnerHTML.</action>
  <verify>
    <automated>cd packages/nextjs && npm run check-types && npm run lint && npm run build</automated>
  </verify>
  <done>All four components typecheck, lint, and build; TokenIcon falls back to a monogram on img error; AddressCopyButton copies the full address and toasts; PairBadge and RegistryHero match the UI-SPEC color/typography contract.</done>
</task>

<task type="auto">
  <name>Task 3: Full engraving PairCard + wire hero into the page</name>
  <files>packages/nextjs/components/registry/PairCard.tsx, packages/nextjs/app/page.tsx</files>
  <action>Upgrade the minimal `PairCard` from 02-01 to the full engraving layout in 02-UI-SPEC PairCard: section 1 = `TokenIcon` (46px) + `cSymbol ⇄ symbol` (Gelasio 17px / JetBrains Mono 13px suffix) + italic name (truncated ellipsis) + `PairBadge`; dashed divider; section 2 = an ERC-20 row (`ERC-20` label + `AddressCopyButton` for the underlying address) and an ERC-7984 row (`ERC-7984 ◈` blue label + confidential-variant `AddressCopyButton`); dashed divider; section 3 = `decimals {n}` + a `Wrap →` CTA. Card: 2px `--line` border, `--panel` bg, padding 20px, gap 14px, hover `translate(-2px,-2px)` + `--shadow`. Revoked pairs render at `opacity:0.6` with the CTA disabled showing `Unavailable` (not-allowed cursor). The `Wrap →` CTA stays visible per the locked design but routes to a disabled/placeholder target this phase (wrap ships in Phase 4) — keep it inert here, do not remove it (UI-SPEC Interaction Contract). Show each side's own decimals separately (underlying vs confidential may differ — Pitfall 4). In `app/page.tsx`, render `RegistryHero` above the grid with `pairCount` bound to `useRegistryPairs().validCount`, keeping the grid and ChainGuard wiring from 02-01.</action>
  <verify>
    <automated>cd packages/nextjs && npm run check-types && npm run lint && npm run build</automated>
  </verify>
  <done>Full engraving cards render both addresses (copyable) + symbol/name/decimals + valid/revoked badge + icon; revoked cards dim with a disabled CTA; the hero shows the live pair count; build/typecheck/lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public RPC -> client | Untrusted token name/symbol rendered inside cards |
| self-hosted assets -> browser | Same-origin icon/hero media under COEP require-corp |
| clipboard | Address strings written on user click |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering/Injection | PairCard / RegistryHero token metadata render | medium | mitigate | React escaping only; no dangerouslySetInnerHTML; truncate name/symbol display length |
| T-02-02 | Spoofing | homograph/spoofed symbol impersonating a real token | low | mitigate | Onchain official registry is source of truth; full copyable address shown so identity is verifiable, not just symbol |
| T-02-05 | Information Disclosure | cross-origin media blocked under COEP require-corp | low | mitigate | All icons + hero self-hosted in public/ (same-origin), no CDN image references (DIF-02) |
</threat_model>

<verification>
- `next build` clean; all 7 icons under public/icons/ are web-optimized (<200KB) and the hero is compressed.
- Local run: the 7 branded cTokens show real engraving icons; the 2 non-branded onchain pairs show monogram tiles (no broken images); cards match the parchment + cellar themes; copy buttons write the full address and toast.
- No cross-origin image requests in the network tab (COEP require-corp holds).
</verification>

<success_criteria>
REG-04 (all 7 cTokens surface with their real self-hosted icons) and REG-06 (each card shows both-network addresses + symbol/name/decimals + valid/revoked badge) are met in the locked Cellar Registry engraving design, with self-hosted, size-optimized media that survives COEP.
</success_criteria>

<output>
Create `.planning/phases/02-registry-browse/02-02-SUMMARY.md` when done.
</output>
