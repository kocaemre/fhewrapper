---
phase: 02-registry-browse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/nextjs/package.json
  - packages/nextjs/vitest.config.ts
  - packages/nextjs/lib/tokenSymbol.ts
  - packages/nextjs/lib/tokenSymbol.test.ts
  - packages/nextjs/lib/mergePairs.ts
  - packages/nextjs/lib/mergePairs.test.ts
  - packages/nextjs/lib/regroupMeta.ts
  - packages/nextjs/lib/regroupMeta.test.ts
  - packages/nextjs/registry/abis.ts
  - packages/nextjs/registry/addresses.ts
  - packages/nextjs/registry/types.ts
  - packages/nextjs/registry/pairs.config.ts
  - packages/nextjs/hooks/useRegistryPairs.ts
  - packages/nextjs/app/page.tsx
  - packages/nextjs/components/registry/PairGrid.tsx
  - packages/nextjs/components/registry/PairCard.tsx
autonomous: false
requirements: [REG-01, REG-02, REG-03, REG-04, REG-05, REG-06]
user_setup: []

must_haves:
  truths:
    - "On the live/local URL, every onchain registry pair (currently 9, including the 7 branded cTokens) renders with real symbol/name/decimals resolved via a single multicall batch"
    - "Revoked pairs are retained in the dataset and flagged isValid=false (never silently dropped)"
    - "A pair declared in registry/pairs.config.ts merges deduped by ERC-7984 address, with the onchain entry winning on conflict"
  artifacts:
    - packages/nextjs/hooks/useRegistryPairs.ts
    - packages/nextjs/lib/mergePairs.ts
    - packages/nextjs/lib/tokenSymbol.ts
    - packages/nextjs/lib/regroupMeta.ts
    - packages/nextjs/registry/abis.ts
    - packages/nextjs/registry/pairs.config.ts
    - packages/nextjs/app/page.tsx
  key_links:
    - "useRegistryPairs reads getTokenConfidentialTokenPairs from the registry at 0x2f0750Bbb0A246059d80e94c454586a7F27a128e, then batches symbol/name/decimals for both sides via useReadContracts (Multicall3)"
    - "regroupMeta chunks the flat 6xN multicall result back onto each pair with per-call allowFailure guards"
    - "mergePairs dedup key = confidential address lowercased; onchain entries written last so they win"
---

<objective>
Deliver the thin end-to-end registry slice: read the onchain Sepolia Wrappers Registry, resolve real token metadata via one multicall, merge a local overlay config, and render every pair on the page with real data. This is the tested data engine (`useRegistryPairs`) plus a minimal-but-real card grid — after this plan a judge sees all onchain pairs on the live URL.

Purpose: Satisfies REG-01/02/03/04/05/06 at the data layer and proves the registry read + multicall + merge works end-to-end with no FHE/WASM dependency (CONTEXT decision A). The three pure functions (normalize, merge, regroup) are the correctness-critical logic and are locked by unit tests first.
Output: A verified minimal registry ABI, a `useRegistryPairs` hook, three tested pure functions, a typed `pairs.config.ts` overlay, and a minimal `PairGrid`/`PairCard` rendering live pairs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-registry-browse/02-RESEARCH.md
@.planning/phases/02-registry-browse/02-CONTEXT.md
@.claude/CLAUDE.md

# Existing code this plan builds on / edits
@packages/nextjs/app/page.tsx
@packages/nextjs/components/ChainGuard.tsx
@packages/nextjs/scaffold.config.ts
@packages/nextjs/package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — verify vitest before install</name>
  <what-built>This phase adds one dev dependency, `vitest`, as the test runner for the three pure functions (no runtime package added). RESEARCH.md's Package Legitimacy Audit predates this decision, so `vitest` is treated as [ASSUMED] and must be human-verified before install (legitimacy checkpoints are never auto-approved).</what-built>
  <action>Pause for human package-legitimacy verification of the pinned `vitest` devDependency BEFORE any install runs in Task 2. Do not install anything until the human approves. This gate is never auto-approved (workflow.auto_advance is ignored for legitimacy checkpoints).</action>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/vitest — confirm it is the official Vitest test runner (maintained by the Vitest team / VoidZero), high weekly download count, recent publish date.
    2. Confirm the version to be pinned exists and is a stable (non-prerelease) release (current stable is the 3.2.x line).
    3. Confirm no typosquat: the package name is exactly `vitest` (not `vi-test`, `vitesst`, etc.).
  </how-to-verify>
  <resume-signal>Type "approved" to allow the pinned `vitest` devDependency install, or describe concerns.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test-locked pure functions + verified registry ABI/config</name>
  <files>packages/nextjs/package.json, packages/nextjs/vitest.config.ts, packages/nextjs/lib/tokenSymbol.ts, packages/nextjs/lib/tokenSymbol.test.ts, packages/nextjs/lib/mergePairs.ts, packages/nextjs/lib/mergePairs.test.ts, packages/nextjs/lib/regroupMeta.ts, packages/nextjs/lib/regroupMeta.test.ts, packages/nextjs/registry/abis.ts, packages/nextjs/registry/addresses.ts, packages/nextjs/registry/types.ts, packages/nextjs/registry/pairs.config.ts</files>
  <behavior>
    - tokenSymbol.iconKey: "USDCMock" maps to "usdc"; "cUSDCMock" maps to "usdc"; "cWETHMock"->"weth"; "ctGBPMock"->"tgbp"; "cXAUtMock"->"xaut"; "cBRONMock"->"bron"; "cZAMAMock"->"zama"; "cUSDTMock"->"usdt". iconFor(confidentialSymbol) returns "/icons/c{key}.png" (e.g. "/icons/cusdc.png").
    - tokenSymbol.truncateAddress: a 42-char 0x address becomes first-6 + ellipsis + last-4 (e.g. begins "0x" + 4 hex, ends 4 hex).
    - mergePairs: given onchain pairs + local pairs, output is deduped by lowercased confidential (ERC-7984) address; when both sides declare the same confidential address, the onchain entry is kept (onchain wins) — REG-05. A local-only confidential address is included. isValid=false pairs are retained in the output (not filtered) — REG-02.
    - regroupMeta: given the flat multicall result array (length 6 x N, entries shaped {status,result}) and the N pairs, output regroups into N enriched pairs each carrying underlying + confidential {symbol,name,decimals}. A per-call status !== "success" falls back (missing symbol/name -> truncated address; missing decimals -> flagged, not thrown) — REG-03 / Pitfall 5.
  </behavior>
  <action>Install `vitest` as a pinned devDependency (approved in Task 1) and add a `test` script running `vitest run` plus `test:watch` running `vitest`. Create `vitest.config.ts` with a Node test environment and an include glob covering `lib/**/*.test.ts` (pure functions only — no jsdom/React harness needed this plan). Write the three `*.test.ts` files FIRST so they fail (RED), importing from the not-yet-created sibling modules via relative paths, then implement the modules to pass (GREEN). Use the live-verified fixtures in RESEARCH.md's "Live-verified pair set" for the merge/regroup tests, including one revoked and one duplicate-confidential-address case. Implement `lib/tokenSymbol.ts` with `iconKey`/`iconFor`/`normalizeSymbol`/`truncateAddress` exactly per RESEARCH Pattern 3 (strip a trailing Mock suffix case-insensitively, strip a single leading lowercase c, lowercase) — this is the core Pitfall-2 fix. Implement `lib/mergePairs.ts` per RESEARCH Pattern 2 (Map keyed by lowercased confidential address, local written first, onchain overwrites) and `lib/regroupMeta.ts` per RESEARCH Pattern 1 (chunk the flat result in groups of 6, guard each entry on status). Create `registry/abis.ts` exporting the verified minimal `registryAbi` (getTokenConfidentialTokenPairs + length + slice + getConfidentialTokenAddress + isConfidentialTokenValid) and `erc20MetadataAbi` (symbol/name/decimals) transcribed verbatim from RESEARCH.md "Code Examples" — do NOT invent selectors (anti-hallucination rule, CONTEXT decision A). Create `registry/addresses.ts` exporting `REGISTRY_ADDRESS` (Sepolia 0x2f0750Bbb0A246059d80e94c454586a7F27a128e) keyed by chainId. Create `registry/types.ts` exporting `TokenMeta`, `RegistryPair` (underlying + confidential TokenMeta, isValid, source), and `LocalPair`. Create `registry/pairs.config.ts` exporting a typed `localPairs: LocalPair[]` initialized empty, with a commented example entry and a header note that onchain entries win on confidential-address conflict (REG-05). Validate any local address inputs with viem `isAddress`/`getAddress` (V5 input validation).</action>
  <verify>
    <automated>cd packages/nextjs && npm test && npm run check-types</automated>
  </verify>
  <done>`npm test` passes all three test files (RED history then GREEN); `iconKey("cUSDCMock")` === "usdc"; mergePairs dedups onchain-wins and keeps revoked; regroupMeta regroups 6-per-pair with allowFailure fallbacks; `check-types` clean; the registry ABI matches RESEARCH.md verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: useRegistryPairs hook + minimal live render</name>
  <files>packages/nextjs/hooks/useRegistryPairs.ts, packages/nextjs/app/page.tsx, packages/nextjs/components/registry/PairGrid.tsx, packages/nextjs/components/registry/PairCard.tsx</files>
  <action>Create `hooks/useRegistryPairs.ts`: step 1 `useReadContract` on `getTokenConfidentialTokenPairs` at `REGISTRY_ADDRESS` (chainId sepolia) — one RPC call; step 2 build the flat multicall list (6 reads per pair: symbol/name/decimals for `tokenAddress` and for `confidentialTokenAddress`) and read via one `useReadContracts` with `allowFailure: true`, gated `query.enabled` on the pairs result; step 3 pipe the flat result through `regroupMeta`, then `mergePairs` with `localPairs`, returning `{ pairs: RegistryPair[], validCount, isLoading, isError, refetch }`. Never fire per-token reads (Pitfall 1 — the keyless public RPC rate-limits; multicall is mandatory, CONTEXT decision A). Create a minimal unstyled `PairGrid` (maps pairs to PairCard) and a minimal `PairCard` showing, per pair: confidential symbol and underlying symbol, name, both decimals, both full addresses, and a plain text valid/revoked marker driven by isValid — REAL data only, no theme/icons/search yet (those land in 02-02/02-03). Replace the walking-skeleton body of `app/page.tsx` so that inside `ChainGuard` it calls `useRegistryPairs` and renders `PairGrid` (leave the client-only provider tree and ChainGuard untouched). Treat onchain `name`/`symbol` as untrusted display strings — render through normal JSX (React escaping), never dangerouslySetInnerHTML (T-02-01).</action>
  <verify>
    <automated>cd packages/nextjs && npm run check-types && npm run lint && npm run build</automated>
  </verify>
  <done>`next build` succeeds; the page compiles and, when run locally on Sepolia, renders all onchain pairs (>=9, all 7 branded cToken confidential addresses present) with real symbol/name/decimals and both addresses; `useReadContracts` is used (single multicall, not N reads); typecheck and lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public RPC -> client | Untrusted onchain token metadata (name/symbol/decimals) crosses into the DOM |
| local config -> client | Developer-supplied addresses in pairs.config.ts parsed at runtime |
| npm registry -> build | New devDependency (vitest) enters the toolchain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering/Injection | PairCard render of token name/symbol | medium | mitigate | React default escaping only; no dangerouslySetInnerHTML; clamp/truncate display length |
| T-02-03 | Tampering | public Sepolia RPC returns manipulated read data | low | accept | Read-only testnet display; reads go through canonical Multicall3; full addresses shown so users verify identity |
| T-02-04 | Denial of Service (self) | invalid address in pairs.config.ts | low | mitigate | viem isAddress/getAddress guard on local entries + allowFailure multicall; skip bad entries with a console warning |
| T-02-SC | Tampering (supply chain) | `vitest` devDependency install | high | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifying npmjs listing before install; version pinned |
</threat_model>

<verification>
- `npm test` green (three pure-function suites); `npm run check-types`, `npm run lint`, `npm run build` all clean.
- Local run on a Sepolia-connected wallet: the page lists every onchain pair with real metadata; the 7 branded cToken confidential addresses from CLAUDE.md all appear; pair count is the live array length (currently 9), not a hardcoded 7 (Pitfall 3).
- Network tab shows a single Multicall3 aggregate call for metadata, not one call per token (Pitfall 1).
</verification>

<success_criteria>
REG-01 (onchain read, never hardcoded), REG-02 (revoked retained + flagged), REG-03 (metadata via multicall), REG-04 (all 7 cTokens present in output), REG-05 (hybrid merge, onchain wins), and REG-06 (card shows both addresses + metadata + status) are satisfied at the data + minimal-render layer; the three pure functions are locked by passing unit tests.
</success_criteria>

<output>
Create `.planning/phases/02-registry-browse/02-01-SUMMARY.md` when done.
</output>
