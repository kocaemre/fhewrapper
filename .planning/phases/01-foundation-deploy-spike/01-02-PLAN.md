---
phase: 01-foundation-deploy-spike
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - packages/nextjs/components/ChainGuard.tsx
  - packages/nextjs/app/page.tsx
autonomous: true
requirements: [FND-02, FND-03]
must_haves:
  truths:
    - "The home route renders a runtime-ready shell showing the live crossOriginIsolated state (not the FHE-counter demo)"
    - "When connected off Sepolia, the app shows a 'switch to Sepolia' prompt and disables actions until chainId === 11155111"
    - "When on Sepolia, the shell renders inside the preserved client-only FHE provider tree"
  artifacts:
    - "packages/nextjs/components/ChainGuard.tsx (chainId === 11155111 gate + useSwitchChain)"
    - "packages/nextjs/app/page.tsx (runtime-ready shell; FHE-counter demo removed; wrapped by ChainGuard)"
  key_links:
    - "wagmi useChainId/useSwitchChain -> ChainGuard -> gates the page shell (off-Sepolia disables actions)"
    - "page.tsx stays under DappWrapperWithProviders (from layout) -> provider mounts client-only"
---

## Phase Goal

**As a** bounty judge, **I want to** open a live public Sepolia dApp URL that is cross-origin
isolated and connect my wallet behind a Sepolia network guard, **so that** the deploy / SSR /
COOP-COEP risk is proven dead before any feature is built on top of it.

> Walking-skeleton slice 2 of 3: the interactive shell + Sepolia chain guard.

<objective>
Turn the scaffolded template into the walking-skeleton's interactive shell: strip the
FHE-counter demo down to a "runtime ready" page that surfaces `crossOriginIsolated`, and add
an explicit Sepolia chain guard that disables actions on the wrong network.

Purpose: Deliver FND-02 (wallet connect is inherited from RainbowKit; the chain-id guard is the
piece the template lacks — its wagmi config auto-adds mainnet + hardhat, so RainbowKit alone will
not flag a wrong network) and keep FND-03 intact (the page renders inside the preserved
client-only provider tree). After this plan the app is deploy-ready for the live verification in
Plan 03.

Output: A `ChainGuard` component and a minimal `app/page.tsx` shell, both building clean.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-deploy-spike/01-RESEARCH.md
@.planning/phases/01-foundation-deploy-spike/SKELETON.md

<interfaces>
<!-- Executor: exact chain-guard + shell snippets are in 01-RESEARCH.md Pattern 3. -->

wagmi hooks (from `wagmi`, already a template dep):
- useAccount()      -> { isConnected, address }
- useChainId()      -> number (current wallet chain id)
- useSwitchChain()  -> { switchChain, isPending }
- sepolia.id === 11155111  (import { sepolia } from "wagmi/chains")

Template components to reuse (do NOT rebuild):
- components/helper/RainbowKitCustomConnectButton  (wallet connect UI, already wired to wagmi)
- components/DappWrapperWithProviders.tsx           (renders the provider tree via app/layout.tsx — leave unchanged)

The FHE runtime does NOT need an encrypt/decrypt round-trip this phase (01-RESEARCH.md Open
Question #1): the thinnest slice proves the provider mounts + the page is cross-origin isolated.
Reading `window.crossOriginIsolated` in the shell is sufficient evidence.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChainGuard component (Sepolia gate + switch)</name>
  <files>packages/nextjs/components/ChainGuard.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-deploy-spike/01-RESEARCH.md (Pattern 3 — the exact useChainId/useSwitchChain snippet; Anti-Patterns; Security Domain wrong-network row)
  </read_first>
  <behavior>
    - Not connected: renders the RainbowKit connect button (delegates to children's connect path or a passed prop), no Sepolia assertion yet.
    - Connected but chainId !== 11155111: renders a "Wrong network — switch to Sepolia" control wired to `switchChain({ chainId: sepolia.id })`, disabled while `isPending`; the guarded children/actions are NOT rendered (disabled).
    - Connected and chainId === 11155111: renders `children` (the guarded content) normally.
  </behavior>
  <action>
    Create a `'use client'` `ChainGuard` component that wraps guarded content. Use `useAccount`,
    `useChainId`, and `useSwitchChain` from wagmi and `sepolia` from `wagmi/chains`. Compare against
    `sepolia.id` (11155111) — do NOT hardcode the number as a bare literal; derive it from the
    imported chain object. When off Sepolia, render a switch-network prompt button bound to
    `switchChain({ chainId: sepolia.id })` and disabled while `isPending`, and do not render the
    guarded children (this is the "disable actions until on Sepolia" behavior of FND-02). Export
    `ChainGuard` as a named export accepting `children`. Keep it presentation-light (Phase 7 themes it).
  </action>
  <verify>
    <automated>cd packages/nextjs && grep -c "useSwitchChain" components/ChainGuard.tsx && grep -c "sepolia" components/ChainGuard.tsx && npm run check-types</automated>
  </verify>
  <acceptance_criteria>
    - `ChainGuard` is a `'use client'` named export using useChainId + useSwitchChain + the `sepolia` chain object
    - Off-Sepolia path renders a switch-network control (disabled while pending) and withholds guarded children
    - On-Sepolia path renders children
    - `npm run check-types` passes
  </acceptance_criteria>
  <done>ChainGuard gates content on chainId === 11155111 with a switch-network prompt; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Runtime-ready shell in app/page.tsx</name>
  <files>packages/nextjs/app/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-deploy-spike/01-RESEARCH.md (Pattern 3 shell example; ## Recommended Project Structure — "STRIP FHE-counter demo -> runtime ready shell")
    - packages/nextjs/app/page.tsx (the template's shipped demo page — read before editing)
    - packages/nextjs/components/ChainGuard.tsx (from Task 1)
  </read_first>
  <action>
    Replace the template's FHE-counter demo UI in `app/page.tsx` with a minimal `'use client'`
    "runtime ready" shell: render the RainbowKit connect button when not connected; once connected,
    wrap the content in `ChainGuard`; inside the guard render a small status line that reads and
    displays the live cross-origin isolation state via `typeof window !== "undefined" &&
    window.crossOriginIsolated` (e.g. "FHE runtime shell ready · crossOriginIsolated: <value>").
    Remove the counter demo's imports and any of its now-unused SDK-call helpers so the page no
    longer references the removed demo module. Do NOT touch `app/layout.tsx` or
    `DappWrapperWithProviders.tsx` — the page must keep rendering inside the inherited provider tree.
    Keep the page free of any module-scope SDK init.
  </action>
  <verify>
    <automated>cd packages/nextjs && grep -c "crossOriginIsolated" app/page.tsx && grep -c "ChainGuard" app/page.tsx && npm run check-types && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx renders the runtime-ready shell (displays `crossOriginIsolated`) and wraps connected content in `ChainGuard`
    - The FHE-counter demo UI and its imports are gone (page references no removed demo module)
    - app/layout.tsx and DappWrapperWithProviders.tsx are unchanged
    - `npm run check-types` and `npm run build` both exit 0 (no SSR / `window is not defined` error)
  </acceptance_criteria>
  <done>Home route renders the runtime-ready shell (crossOriginIsolated status) inside ChainGuard; demo removed; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> injected wallet | User signs/sends on whatever chain the wallet is set to |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Spoofing/Tampering | Wrong-network signing (wallet on mainnet/hardhat, not Sepolia) | medium | mitigate | Explicit `chainId === 11155111` guard (ChainGuard) beyond RainbowKit; guarded actions withheld and a switch prompt shown until on Sepolia (FND-02) |
| T-01-03 | Tampering | Cross-origin resource injection under COEP | low | accept | `require-corp` (set in Plan 01) + no cross-origin subresources this phase; revisit when media is added (DIF-02 mandates self-hosting) |
</threat_model>

<verification>
- `npm run build` + `npm run check-types` clean (FND-03 preserved through the edited page).
- ChainGuard uses useSwitchChain + the `sepolia` chain object (positive grep) — FND-02 guard present.
- page.tsx displays `crossOriginIsolated` and wraps content in ChainGuard (positive grep).
</verification>

<success_criteria>
- Home route renders the runtime-ready shell (not the counter demo) inside the client-only provider tree.
- Off-Sepolia shows a switch prompt and disables actions; on-Sepolia renders the shell.
- Build + typecheck clean.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-deploy-spike/01-02-SUMMARY.md` when done.
Record: the ChainGuard export signature and the shell's isolation-status behavior for Plan 03's live check.
</output>
